# 面试问题清单

> 共 48 题，覆盖 RAG、SSE 异步、MCP 协议、数据库缓存、鉴权安全、工程化、开放场景 7 个维度。

---

## 一、RAG & 检索（8 题）

1. 讲讲 RAG 的完整流程，每一步做了什么？
2. 文本切分器为什么要按 `\n\n → \n → 。→ . → 空格` 这个优先级？顺序换一下会怎样？
3. chunk_size 和 chunk_overlap 怎么定的？如果 overlap 太大或太小会有什么问题？
4. 为什么用 Chroma？跟 Faiss / Milvus / pgvector 对比过吗？
5. 余弦相似度的 distance 阈值设的多少？怎么确定这个值的？
6. 检索出来的文档怎么拼进 Prompt？如果检索结果太长超过 token 限制怎么办？
7. 来源多样性去重（每文档最多 3 条）为什么要这么做？不做会出什么问题？
8. Embedding 模型选的哪个？为什么选这个？对比过其他模型吗？

---

## 二、SSE & 异步（7 题）

9. 为什么用 SSE 而不是 WebSocket？什么场景下该用哪个？
10. "双层 finally + CancelledError"具体是怎么实现的？能不能画一下流程？
11. 客户端断开后，如果不处理 CancelledError，会发生什么？
12. `asyncio.to_thread()` 是什么？为什么要把 DB 操作扔进去？
13. 你在 async 函数里用 sync SQLAlchemy，为什么不直接用 async SQLAlchemy？
14. FastAPI 的 `StreamingResponse` 底层是怎么工作的？
15. aiohttp 连接池怎么管理的？有没有遇到过连接泄漏？

---

## 三、MCP 协议（9 题）

16. MCP 是什么？用自己的话讲清楚。
17. MCP 和 REST API 的区别是什么？
18. MCP 和 OpenAI 的 Function Calling 有什么区别？
19. MCP Server 为什么用 stdio 通信而不是 HTTP？
20. MCP 的 `tools/list` 和 `tools/call` 分别干什么的？
21. 你的 MCP Server 是怎么被 Claude Code 发现并连接的？配置文件长什么样？
22. 三个 MCP 工具的粒度怎么权衡的？为什么是 3 个而不是 1 个或 10 个？
23. 如果 MCP Server 挂了，Claude Code 会怎样？
24. 除了 Claude Code，你的 MCP Server 还能被谁调用？

---

## 四、数据库 & 缓存（5 题）

25. Redis 在你的项目里起什么作用？为什么不用数据库直接查历史？
26. LPUSH + EXPIRE 30 分钟——为什么是 30 分钟？10 分钟或 1 小时有什么问题？
27. Redis 挂了你的服务还能正常工作吗？怎么处理的？
28. SQLite 和 Redis 双写，数据一致性怎么保证？
29. SQLAlchemy ORM 的 session 生命周期是怎样的？什么时候 commit？什么时候 rollback？

---

## 五、鉴权 & 安全（5 题）

30. X-API-Key 鉴权是怎么实现的？为什么不用 JWT 或 OAuth？
31. 如果有人抓包拿到了 API Key，你怎么防御？
32. `.env` 泄露了会怎样？你怎么防止的？
33. CORS 配置的 `allow_origins=["*"]` 有什么风险？
34. 用户 A 能不能查到用户 B 的聊天历史？你应该怎么防止？

---

## 六、工程化 & 测试（6 题）

35. 你写的 28 条 pytest 用例覆盖了哪些场景？有没有 mock？
36. 为什么只测了 text_splitter 和 prompts，没测路由层？
37. Claude Code 的 Skill 是什么？跟 MCP Tool 有什么区别？
38. 如果让你在 CI/CD 里集成 pytest，你会怎么做？
39. 项目日志是怎么配置的？出了线上问题你怎么排查？
40. `requirements.txt` 里的版本号是锁定的还是浮动的？为什么？

---

## 七、开放 & 场景（8 题）

41. 这个项目如果要从 100 个用户扩展到 10000 个，哪里会先崩？你怎么改？
42. 如果用户上传了一个 100MB 的 PDF，你的系统会怎样？
43. 用户问了一个知识库里没有的问题，你的系统怎么回答的？
44. 如果 DeepSeek API 挂了，你的 `/chat` 接口返回什么？
45. 你觉得这个项目最大的技术难点是什么？
46. 如果重新做这个项目，你会改哪些设计决策？
47. 最近半年 AI 行业发生了什么？你觉得 Agent 方向接下来会怎么发展？
48. 你对我们公司的业务有了解吗？你觉得你的 RAG 系统能在我们这怎么用？

---

> 使用建议：对着每道题先自己说一遍（录音），再跟答案对照。卡住的题记下来补，说顺了就不用再看答案了。
