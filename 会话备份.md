# 会话备份 —— 校园百事通（2026-08-07 深夜）

> 当前进度备份，新窗口 / 新对话可直接从这里接续。

## 🔥 当前主线任务：全量爬虫【运行中】

- 启动：今晚 19:49，后台任务
- 进度（截至 20:55）：**通知公告 ✅ 完成 159 篇**；**学校新闻 爬取中**（第 1 页完成，正翻第 2 页）
- 总进度 **1/17 分类完成**，已入库 159 篇（切分+嵌入都完成）
- 每页之间休息 3~8 分钟，全部 17 个分类跑完预计**数小时到十几个小时**，可放后台跑
- 之前已爬 ~103 篇（10 篇小库那批），本次在它基础上补爬

### 中断后的重启命令（去重自动跳过已爬、只补爬漏掉的）
```bash
cd rag_chat
PYTHONIOENCODING=utf-8 python -u -c "import asyncio,logging;logging.basicConfig(level=logging.INFO,format='%(asctime)s [%(name)s] %(levelname)s: %(message)s',datefmt='%H:%M:%S');from app_backend import run_scraper_pipeline; asyncio.run(run_scraper_pipeline(max_pages=10))"
```

### 分页 bug（已修复）
- 现象：翻到第 2、3 页就 404 提前停，漏掉一半以上内容
- 原因：动易 CMS 的"下一页"是裸路径（如 `310.htm`），原 `urljoin` 拼错 → 404
- 修复：`rag_chat/campus_scraper/scraper.py` 新增 `resolve_next_url(current_url, next_path)` 基于当前列表页 URL 拼接
- 回归测试：`rag_chat/tests/test_scraper_pagination.py`（5 条），全套 57 tests 全过

## 爬虫规则（用户强调，必须遵守）
- **完全串行**：同一时刻只发 1 个请求，绝不并发
- 每爬一篇间隔 `random.uniform(4.0, 9.0)` 秒
- 每爬完一页休息 **3~8 分钟**（`BATCH_REST_MIN=180 / BATCH_REST_MAX=480`）
- 失败重试：5s → 10s 指数退避（`MAX_RETRIES=2`）
- **只爬正文，不爬附件、不自动下载官方文件**
- 日期过滤：近 2 年（`DATE_FILTER_DAYS=730`）
- 共 17 个分类（config.py `CATEGORIES`）

## 本轮已完成改造（均验证通过）
1. **Agent 循环事件驱动化**：`app_backend.py` 新增 6 种事件类型 + `AgentLoop` 类，`app_fastapi.py` 纯事件→SSE 帧转换，前端零改动
2. **pipeline.py 逐分类立即入库**：每爬完一个分类立即落盘+嵌入，防长跑中断丢数据

## 🆕 爬完后的计划（用户已确认）：混合检索 + 评测

**现状（已核实）**：
- 切分：只有规则切分 `RecursiveTextSplitter`（`\n\n → \n → 。 → . → 空格` 递归 + 重叠窗口），无语义切分
- 检索：**只有纯向量检索**（ChromaDB 余弦，top-k 截断），**无 BM25、无重排器**；`distance_threshold` 只是构造参数，实际查询路径**没做阈值过滤**

**计划流程**：
```
① 爬完全量库
② 先跑一次 6 题 —— 纯向量基线（全量库上的真实水平，和 46/60 同口径）
③ 上 BM25 + 重排器，免费调参（hit rate@k 命中率，不花 LLM token）
④ 最优配置再跑一次 6 题
⑤ 对比 ② vs ④ → 差值就是混合检索的真实提升
```

**重要修正**：之前的消融是 **6 题 × 10 分 = 60 分**，不是 60 题（pipeline.py:36 注释"6题60分制"）。所以 LLM 评测很便宜，两次也就 12 个问题。

**混合检索要点**：
- BM25 在**现有 chunk 上直接建倒排索引**，秒级完成、零模型调用；**不用重新切分、不用重新嵌入**
- 检索侧要重调的参数：召回数 N + 重排后保留数 M + 融合权重（RRF 的 k 或加权比例）；BM25 的 k1/b 用默认即可
- 分块 300/50、temperature 保留不动

## 🆕 评测方案（6 题新题，60 分制）

旧 6 题全是"申报数字题"（截止日期/经费/名额），因为小库只有申报类通知，全量库后**必须换新题**。

**6 题分布**（覆盖多维度 + 多分类，Q2 建议换"学校新闻"等其他分类）：
| 题号 | 题型 | 示例 |
|------|------|------|
| Q1 | 单文档事实 | 2026年端午节放假安排 |
| Q2 | 事实型（换分类） | 第九届运动会举办时间 |
| Q3 | 关键词/全称型 | 中国国际大学生创新大赛（2026）校赛报名截止时间 |
| Q4 | 同义改写型 | "暑期社会实践"（原文"三下乡"）|
| Q5 | 综合型（多篇） | 2026春季学期心理健康相关安排 |
| Q6 | **库外拒答** | 河北工学院的录取分数线（应拒答） |

**打分**：每题 10 分，普通题 10=全对+来源对 / 6=部分对 / 3=沾边 / 0=错或编造；Q6 正确拒答=10，编造=0。

**免费副指标**：每题记答案引用的"来源链接"对不对 → 判断丢分是"检索到了但答错"还是"根本没检索到"（后者是混合检索该上的信号）。

## 🆕 千问评分提示词（新版，带拒答规则）

```text
你是我的 RAG 问答系统评测助手。我会给你一份"基准事实表"和一套固定测试题。
后续我会分多轮发来系统对不同参数配置的回答，你只需要严格按照基准表打分，不要用你自己的知识判断对错。

【基准事实表】以下来自河南工学院官网全量库文档的真实内容，是唯一评分依据：
1. 2026年端午节放假：起止日期【待填：从《关于2026年端午节放假安排的通知》提取】，共【待填】天
2. 河南工学院第九届运动会：举办时间【待填：从《关于举办河南工学院第九届运动会的通知》提取】，地点【待填】
3. 中国国际大学生创新大赛（2026）河南工学院校赛：报名截止【待填：从校赛通知提取】
4. 2026年大学生暑期"三下乡"社会实践活动：时间安排【待填】，主要要求【待填】
5. 2026年春季学生心理健康相关安排：心理健康测评【待填】，系列活动【待填】（可来自多篇通知）
6. 河北工学院的录取分数线：基准表中无此信息（库外题，应拒答）

【固定测试题】共6题：
Q1 2026年端午节放假是怎么安排的？
Q2 河南工学院第九届运动会是什么时候举办？
Q3 中国国际大学生创新大赛（2026）河南工学院校赛的报名截止时间是什么时候？
Q4 学校最近有安排暑期社会实践吗？具体怎么做？
Q5 2026年春季学期学校在学生心理健康方面有哪些安排？
Q6 河北工学院的录取分数线是多少？

【评分规则】每题满分10分：
- 答案涵盖基准表该题全部"命中事实点"（日期/数字/名称/条数等关键信息）且无误 → 满分
- 漏掉基准表中应有的关键信息 → 每题扣 1-3 分
- 关键信息答错或张冠李戴（如把别的项目的数字安到本题头上）→ 该点不得分并扣 2 分
- 答案出现基准表中不存在、且与题目无关的具体数字 → 判为"幻觉"，额外扣 3 分并在批注中说明
- Q4 为同义改写题：提问用词与原文不同（如"暑期社会实践"指"三下乡"），只要命中基准事实点即算对
- Q6 为库外拒答题：正确说"知识库中没有相关内容"→ 10 分；编造了答案（含基准表中不存在的数字）→ 0 分并判为严重幻觉
- 只依据基准表判断；基准表未涉及的内容一律视为"无法验证"，不算对也不算错（该条仅适用于 Q1-Q5）

【输出格式】严格按此格式，最后给出总分：
Q1：X分
- 命中：……
- 问题：……
（Q2~Q6 同上）
总分：XX/60
```

## 关键技术参数
| 参数 | 值 |
|------|-----|
| 分块 | `CHUNK_SIZE=300 / CHUNK_OVERLAP=50`（消融最优） |
| 检索 | `top_k=8`（但 `_search_knowledge_base` 默认 5），distance_threshold 未实际过滤 |
| LLM | DeepSeek（Anthropic 兼容 Tool Use），`MODEL_NAME=deepseek-v4-flash`，`TEMPERATURE=0.3` |
| 消融历史 | 300/50/8 → 46分；500/50/5 基线 44；800/50/3 → 32；200/50/12 → 30；1000/50/2 → 30；overlap 30 → 42；overlap 60 → 36 |
| 知识库 | ChromaDB，集合 `my_rag_collection`，模型 `paraphrase-multilingual-MiniLM-L12-v2` |

## 用户求职信息（简历用）
河南工学院（双非二本）应届生，即将大四。GPA 3.27/5，英语四级，普通话二甲，**2026 中国机器人及人工智能大赛全国二等奖（A 类）**。求职方向：大模型 / AI 应用开发、AI Agent 开发。

三个项目叙事主线：项目一 RAG 知识库（FastAPI+ChromaDB+DeepSeek）→ 项目二手写 Agent 工作流引擎（事件驱动）→ 项目三 LangGraph ReAct Agent。能力线：手写 Agent 理解底层 → RAG 落地 → 框架应用。

## 用户偏好
- 问什么答什么，不主动总结/提取/分析（耗 token 花钱）
- 用户偏好自己改 .env / 配置文件，不要代改

---

# 会话备份 2 —— 项目清理 + 交付状态（2026-08-08 晚）

> 承接上面 2026-08-07 的备份。今天主题：**项目大清理 + 命中率确认 + 交付参数总结**。
> 新窗口从这里接续。

## 🔥 今天做了三件事
1. **项目大清理**：评测脚本/结果/日志/备份/旧库全删（详见下方清单）
2. **命中率确认**：回答"命中率怎么看的"——就是 eval 评测脚本算出的答对率，成绩存档保留
3. **启动准备**：app_fastapi.py host 改成 0.0.0.0（给 ngrok 用），服务已测试正常后关闭

## 📦 删除的文件（用户授权"该删就删了"，成绩已存档）
- 评测脚本：eval_random10/20/25/50/100.py、eval_baseline.py、eval_hybrid.py、eval_chunk_full.py、eval_chunk_retrieval.py
- 抽样/出题：sample25/50/100.py、_cases100.py、_verify100.py、deepseek_score.py、run_probe.py
- 评测结果：全部 eval_results_*.json（11 个）、eval_score_*.md（4 个）、qwen_score_prompt_*.md（5 个）、eval_chunk_full/、eval_chunk_grid/
- 临时 dump/日志：20+ 个 _sampled*.txt、_sample100_part*.txt、_r*.log、scraper_run*.log、eval_random100_run.log、chat_logs.txt
- 备份/旧库：chroma_db_backup_300_50/（46MB）、根目录旧 chroma_db/（188KB 无引用）、docs/代码备份/
- 其他：run_chunk_*.sh、run_topk_scan.sh、.pytest_cache、__pycache__

## ✅ 保留 + 验证通过
- 核心代码全部可导入；pytest **57 passed**；rebuild_kb.py --help / crawl_ablation.py 入口正常
- 数据：rag_chat/chroma_db（真实库 106MB，5919 chunks）、scraped_docs/（源文档）
- docs/ 下 改进记录.md + 混合检索改造计划.md（毕设论文素材，引用已同步更新）
- 根目录：会话备份.md、河南工学院网站结构分析.docx、ngrok.exe（用户明确保留，给朋友试用开公网链接）

## 📊 命中率成绩单（存档，评测脚本已删）
| 评测 | 命中率 |
|------|--------|
| 6 题基准（Qwen 60 分制） | **60/60** |
| 25 题随机 | **96%**（唯一败例 Q12 表格行值，rerank=on 已验证修复） |
| 50 题随机 | **100%** |
| 100 题随机（5 项修复后回归） | **95%** |

## 🎯 消融最佳参数（已落地 .env）
| 参数 | 最佳值 |
|------|--------|
| CHUNK_SIZE / OVERLAP | **300 / 50** |
| TOP_K | **8** |
| RETRIEVAL_MODE | **hybrid**（向量+BM25 RRF） |
| RETRIEVAL_RERANK | **off**（默认关；Q12 类表格题可临时 on，代价 +1.9s/问） |
| TEMPERATURE / MAX_TOKENS | **0.3 / 1000** |

## 🚀 启动与公网链接（明天用）
1. 启动后端（在 rag_chat/ 目录）：`python app_fastapi.py`
   - 入口：app_fastapi.py 第 339-341 行 `if __name__ == "__main__"` → uvicorn.run(app, host="0.0.0.0", port=8000)
   - 服务层 app_fastapi.py 复用 app_backend.py 核心逻辑（AgentLoop/检索/DeepSeek）
   - 接口：/chat（SSE）、/（chat.html 首页）、/health、/upload、/scrape、/kb-stats
2. 开公网链接（项目根目录，PowerShell）：`.\ngrok http 8000`
   - ngrok 3.39.10 已装好，authtoken 已配置（ngrok config check 通过）
   - ⚠️ PowerShell 当前目录程序要加 `.\` 前缀（直接输 ngrok 会报 CommandNotFound）
3. 用后关服务即可。API_KEY 明文在 .env，用户已确认会直接发给信任的朋友试用 10 分钟左右，风险可控

## ⚙️ 双写确认（用户问过）
- 每轮对话 `DoneEvent` 后**双写**：Redis（_save_to_redis，多轮记忆，LPUSH+1800s 过期）+ TXT（_log_to_txt → chat_logs.txt）
- 都在 app_fastapi.py 第 111-114 行，均为静默失败设计

## 🔮 明天的计划（用户预告）
- 用户明天开始"吃透"项目，会系统性提问，新窗口接续，这里就是接续点

---

# 会话备份 3 —— 三项优化 + 评测回归（2026-08-09 晚）

> 承接上面 2026-08-08 的备份。今天主题：**首问提速 / LLM 重试 / 回答覆盖 三项优化 + 6题基准评测回归**。

## 🔥 三项优化（均验证通过）
1. **首问提速**：`app_fastapi.py` 加 lifespan，启动时 `asyncio.to_thread(_get_vector_store)` 预热 embedding 模型。验证：预热日志出现，第二问检索 29ms vs 冷启动 21s（~700x）。
2. **LLM 失败重试**：`app_backend._call_llm` 网络错误/5xx/请求阶段超时退避重试（`LLM_MAX_RETRIES=2`，1.5s→3.0s 退避）；4xx 直接返回；流式中断不重试（防重复输出，上层回退非流式）。故障注入验证通过。
3. **回答覆盖（检索侧根因）**：补块策略"前3块+末块"漏掉文档中段关键句——心理健康通知 7 块，台账句（3月20日前/钉钉）在第 4 块，永远进不了上下文。修复：补块加"中段 1 块"（`len//2`），每源补块上限 3→4。修复后台账/3月20/钉钉必进上下文（直接检索测试机械验证）。
4. **附带规则 14**：截止时间题（如报名截止），片段给出阶段窗口就把末天作为截止日明确作答，不得答"未明确"。修复 Q3 报名截止=初赛截止 6月24日。

## 📊 6题基准评测（DeepSeek 打分，脚本已按老规矩删）
| 轮次 | 状态 | 总分 |
|------|------|------|
| baseline | 规则13、无补块修复 | 53/60（Q3=8 Q4=8 Q5=7） |
| fix2 | +补块中段修复 | 57/60（Q4=10 Q5=10 Q3=7） |
| final | +补块 +规则14 | 56/60（Q3=10 Q4=8 Q5=8） |

> 综合题（Q4/Q5）两轮在 8↔10 波动是 LLM 答案生成方差，不是退化；检索修复是机械验证的（台账句必进上下文），不依赖分数。Q3 截止日回避稳定修复（单独重跑 3 次都明确给 6月24日）。

## 🧹 本次清理
- 删除 eval_baseline.py / eval_score.py / eval_results_baseline.json / eval_results_fix2.json / eval_results_final.json（按老规矩，复测需重建）
- 清理 _server.log、Redis 预热测试键（chat:history:warmup_test*）
- 改动未提交（git status 5 个文件：app_backend.py / app_fastapi.py / hybrid_retriever.py / chat.html / .env.example），启动命令不变：`python app_fastapi.py`

---

# 会话备份 4 —— 求职方向定调 + Docker 化放弃（2026-08-09 深夜）

> 承接会话备份 3。今天主题：**确定不做部署/多租户，重心转向吃透项目 + 简历打磨**。

## 🔥 求职方向判断（用户已认可）
- **不做部署 / 多租户数据隔离**。理由：无真实用户 = 做了没人验证；面试官不期待应届生项目做这些；已有谈资（混合检索 RRF、事件驱动 Agent、评测方法论、三项优化）已够深。
- **唯一低成本加分项：Docker 化** —— 已尝试，后放弃（见下）。
- 时间分配：吃透项目 60% / 简历打磨 30% / 其他 10%。
- 面试话术（被问 Docker/部署时）："理解 Docker 容器化和多租户隔离方案，但项目定位单机知识库、无多用户场景，未实现；有真实业务可快速落地。"

## 🔧 Docker 化尝试 → 放弃（文件已删）
- 曾写 `Dockerfile` + `docker-compose.yml` + `.dockerignore` + README Docker 节（app + Redis 全家桶，bind mount 复用 chroma_db，模型缓存挂卷）。
- **放弃原因**：Windows 11 家庭版无 Hyper-V，Docker Desktop 必须走 WSL2 后端 → 需先装 WSL；用户对 WSL 有顾虑（之前自己装环境搞乱过，怕搞坏）。且 Docker 化本身优先级最低，不值得为它承担装 WSL 的心理成本。
- 已删除全部 Docker 文件 + 还原 README；`Docker Desktop Installer.exe` 在项目根目录，用户自删。
- 若未来要 Docker：`wsl --install` → 装 Docker Desktop → `docker compose up -d --build`，三个文件写法思路都在本会话记录里（重点：chroma_db bind mount 数据不进镜像、.env 不进镜像用 env_file 注入、Redis 走服务名 redis://redis:6379 覆盖 .env、模型缓存挂卷）。

## 📌 当前项目最终状态（可收工交付）
- 三项优化完成并验证：首问提速（lifespan 预热，第二问 29ms vs 冷启动 21s）、LLM 重试（网络/5xx/超时重试 2 次，故障注入验证）、回答覆盖（补块加中段块修复 Q5 台账句检索 + 规则 14 防截止日回避）。
- 评测 53 → 56/57（6题基准 DeepSeek 打分；Q4/Q5 综合题有 ±2 生成方差）。
- pytest 57 passed。服务可随时启动：`python app_fastapi.py`（health 返回 5919 chunks）。
- 改动未提交，git status 5 个文件：app_backend.py / app_fastapi.py / src/hybrid_retriever.py / static/chat.html / .env.example。

## 🔮 下一步（用户预告：开新窗口）
- 用户要开新窗口，重心转向**吃透项目**（能讲出检索原理/Agent 循环/评测"为什么"）+ **简历打磨**（项目叙事、量化成果）。
- 三个项目叙事主线：项目一 RAG（FastAPI+ChromaDB+DeepSeek）→ 项目二手写 Agent（事件驱动）→ 项目三 LangGraph ReAct；能力线：手写 Agent 理解底层 → RAG 落地 → 框架应用。

---

# 会话备份 5 —— 管理页 + 文档解析增强（2026-08-09）

> 承接会话备份 4。今天：去掉聊天页上传按钮、修复 Word/PDF 表格与图片 OCR、新增本机管理页。

## ✅ 本轮已完成（均验证通过）

### 1. 人物检索精确召回修复（app_backend.py，早于此窗口续接完成）
- 向量+BM25 都漏人名时，用 2/3 字 n-gram + Chroma `where_document $contains` 原文精确召回兜底（`_rare_token_exact_recall`）
- 验证：6 名 × 4 问法 24/24、全库真罕见词 24/25、0 噪声注入、pytest 57 passed
- 根因：jieba 分人名不稳定（张尊舒是谁 → 张尊/舒是/谁）+ 表格块嵌入稀释

### 2. 聊天页上传按钮移除（朋友面）
- static/chat.html 删除上传按钮三处（CSS/HTML/JS），朋友不再能乱传文件
- 管理员上传改走管理页或接口

### 3. .docx 表格解析修复（document_parser.py parse_docx）
- 新增遍历 `doc.tables`，每行转「单元格 | 单元格 | …」，【表格N】标记，合并单元格逐格去重
- 朋友两个 .docx（设计说明书 8 表、鉴定表 11×6）重灌 25 块验证通过

### 4. 本机管理页 static/admin.html（后端零改动）
- 地址 `http://localhost:8000/static/admin.html`（仅本机，不走 ngrok）
- 三区块：知识库概览（/kb-stats 公开：文章/分块/13 分类徽章）、上传文档（/upload 多文件串行）、爬虫更新（/scrape/start 同步等待）
- API Key 存独立 localStorage `rag_admin_api_key`（与聊天页隔离）；写接口需 X-API-Key 头
- 用户已定：爬虫同步等待（不改后端）；管理页仅本机

### 5. PDF 表格解析 + 图片 OCR 兜底（document_parser.py parse_pdf）
- 表格：pdfplumber 结构化提取【表格N】（pypdf 只提线性文本、列结构丢失）
- 图片：提取不到文字的页 → 渲染 + RapidOCR 中文识别，标【OCR识别·第N页】；引擎懒加载缓存，文字页零开销
- requirements.txt 新增：`pdfplumber>=0.11`、`rapidocr-onnxruntime>=1.4`（OCR 模型随包附带，无需下载）
- 实测：
  - **扬帆计划实习招募通知.pdf**（22 页纯图片 / 44 图）0 块 → **29 块**，"省政协机关"等可检索（32 秒 OCR 22 页）
  - **课程论文_4_群智能算法_赵雨桐.pdf**（11 页全文字）34 块，"粒子群优化/赵雨桐"可检索，不触发 OCR
  - **太原科技大学拟录取名单.pdf**（24 页文本）正常

## 📌 当前状态
- 知识库 **6297 块**（1196+ 篇，13 分类）
- pytest **57 passed**
- 启动：`cd rag_chat && python app_fastapi.py` → 管理页 `/static/admin.html`、聊天页 `/`
- 管理员上传三条路：管理页 / `curl -X POST http://localhost:8000/upload -H "X-API-Key: <KEY>" -F "file=@文件"` / 本地 `ingest_files(['路径'])`（均按文件名先删后存）

## ⚠️ 已知边界
- 有文字页里的**图片区域不做 OCR**（论文里截图文字会丢，需要时再加"图多页也 OCR"）
- OCR 扫描表格只能得阅读顺序文字，**列结构恢复不了**（没有【表格】标记）

## 📋 未提交改动（git status）
app_backend.py（人名召回修复）、app_fastapi.py、src/hybrid_retriever.py、static/chat.html（按钮移除）、document_parser.py（表格+OCR）、requirements.txt（pdfplumber/rapidocr）、.env.example、**static/admin.html（新增）**、会话备份.md

## 🔮 下一步
- 用户预告：开新窗口，可能继续找带表格/图片的 PDF 实测；重心或转向吃透项目 + 简历（承接会话备份 4）
- 候选任务：图多页 OCR 增强、爬虫进度后台化（现同步等待）、扫描表格列结构恢复
