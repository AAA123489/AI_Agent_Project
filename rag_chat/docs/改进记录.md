# 项目改进记录（累计变更日志）

> 用于毕业设计论文写作：按时间顺序记录对 RAG 知识库问答系统的全部工程化改造，
> 每条写明 **改了什么 / 为什么改 / 效果**，可对照代码与评测数据。

## 项目背景

- 知识库：河南工学院官网全量爬虫，1195 篇 / 13 分类，分块 CHUNK_SIZE=300 / OVERLAP=50（全量消融最优）。
- 基线：6 题基准评测（60 分制），历史 Qwen **30/60** → 全量消融修复后 **60/60**（最终 300/50/8 由 DeepSeek 按同一评分规则打分）。
- 评测脚本与结果文件（`qwen_score_prompt_baseline*.md` / `eval_results_*.json` 等）曾于 2026-08-08 清理；**6 题基准那套已于 2026-09-16 从 git 历史（`e18707e`）与 `eval_score.pyc` 常量表逐字恢复**为 `rag_chat/eval_baseline.py` + `eval_score.py`，复跑 **60/60**（hybrid / top_k=8 / rerank=off）。
- 25/50/100 题三套随机评测的题库与脚本从未入库，已彻底丢失、无法恢复。
- **重要说明**：Qwen 评分器仅作参考，不作为工程改造的验收标准（用户确认）——检索器功能正确、参考文献正确即达标。

---

## 2026-08-08 ｜ 批次 1：检索质量加固（纯向量链路）

### 1. 库外主体校验闸门（`_refuse_out_of_kb_school`）
- **改了什么**：识别 query 中的学校名（正则「XX大学/XX学院」+ 泛指字过滤），问知识库主体（河南工学院）之外的学校 → 直接拒答，不进检索。
- **为什么**：Q6「河北工学院录取分数线」——库外题若强行检索，LLM 会把主体校分数张冠李戴，编造幻觉数字。
- **效果**：Q6 从 0 分（幻觉）修复到 10 分（正确拒答）。

### 2. 年份过滤（`_extract_year` + Chroma `where={"year": year}`）
- **改了什么**：query 提到 20xx 年份 → 检索时按元数据 year 过滤，只看该年文档。
- **为什么**：跨年旧通知（如 2025 年心理健康工作）会挤占检索名额，污染当年答案。
- **效果**：清除跨年污染，当年通知稳占候选。

### 3. 文本去重（seen set）
- **改了什么**：检索结果按文本内容去重。
- **为什么**：同一通知被爬进两个分类目录 + 重叠窗口 → 产生内容相同的块，重复占名额。
- **效果**：候选不重复，名额留给不同文档。

### 4. 同文档补块（按 source 元数据整篇补齐）
- **改了什么**：对初始命中的每个来源，用 `collection.get(where={"source": src})` 拉该文档全部块，追加未召回的后续块（每来源限补 3 块、最多补 5 篇、总封顶 TOP_K+12=20）。
- **为什么**：宽泛问题（如 Q5「有哪些安排」）常只命中文档开头，关键句（3月3-15日排查、3月20日台账）埋在正文第二、三节，语义相似度捞不到。
- **效果**：Q5 排查通知（2026-03-03）关键事实稳定进上下文（3/3 可靠命中）。

### 5. 综合通知优先提示词（SYSTEM_PROMPT 第 10 条）
- **改了什么**：问「有哪些安排/如何开展/工作怎么做」类概括题时，以覆盖多项工作的综合性通知（标题常含「有关工作的通知」）为作答主干，完整列出各项工作及日期要求。
- **为什么**：Q5 检索到综合通知+单次活动通知时，LLM 按显示相关度锚定到单次《测评通知》（3月11-27），漏掉综合《排查通知》（3月3-15 排查 / 3月20 台账）——非确定性问题，1/3 概率答对。
- **效果**：Q5 可靠性提升到 3/3。

### 6. Q6 拒绝消息强化
- **改了什么**：拒绝文案明确「严禁补充河南工学院或其他学校的任何信息（含录取分数），即使作为附加说明也不行，只拒绝不展开」。
- **为什么**：回归测试发现补块逻辑把录取分数线文档带进上下文，LLM 列出河南工学院分数（531/520）——违反基准表外数字即幻觉的规则。
- **效果**：Q6 稳定拒答，不越界补充。

---

## 2026-08-08 ｜ 批次 2：混合检索改造（BM25 + RRF + 可选重排）

### 背景
纯向量语义检索对"关键词精确命中"类信息（如 Q3「报名截止时间」）鲁棒性不足：关键句埋在文档中段、与 query 语义重叠度低时，余弦相似度经常捞不到。

### 7. 新增依赖
- `jieba 0.42.1`（中文分词）、`rank_bm25 0.2.2`（OKAPI BM25 打分）。轻量纯 Python 包。
- 重排用已装的 `sentence-transformers` 的 `CrossEncoder`，不新增重型依赖。

### 8. 新增 `src/hybrid_retriever.py`（检索三件套，惰性 + 单例缓存）
- `Bm25Retriever`：懒加载全量块（6432 块）jieba 分词建倒排索引；`search(query, top_n, year)` 全库打分 → 取 top30 → 按年过滤 → 截断；distance 由归一化 BM25 分换算，保持"相似度%"展示格式一致。
- `rrf_fuse(vector_results, bm25_results, k=60, top_n)`：Reciprocal Rank Fusion 名次融合，无权重参数、稳健；按 text 去重，重复块保留更高相似度。
- `Reranker`：`BAAI/bge-reranker-base`（中文精排标准，~440MB），仅开关开启才加载；`rerank(query, candidates, top_n)` 对 (query, 文本) 对精排。原计划 mmarco 小模型镜像 404，改为 bge-reranker-base；重排失败时原样返回全部候选（不截断，等价于关掉重排）。

### 9. 主链路接入混合检索（`_search_knowledge_base`）
新流程：库外闸门 → 年份过滤 → 向量+BM25 双路召回 → RRF 融合 top_k → 去重 → 同文档补块 → 可选重排 → 格式化（格式不变，兼容前端 `_parse_sources`）。

新增 `.env` 开关：`RETRIEVAL_MODE=hybrid|vector`、`RETRIEVAL_RERANK=on|off`、`RERANK_TOP_N=8`。
鲁棒性：jieba/rank_bm25 缺失或 BM25 建索引失败 → 自动回退纯向量，不崩。

### 10. 混合检索消融实验
- 同一 6 题，三档：`vector` / `hybrid` / `hybrid_rerank`（脚本 `eval_hybrid.py` 已随清理删除）。
- 每档每题记录**检索级关键句命中**（与 Qwen 评分解耦，只验证检索器功能）：
  - Q3：`报名截止` `6月24日` `6月10日`
  - Q5：`3月3日` `3月15日` `3月20日` `台账`

### 消融结果（2026-08-08）
关键句命中：Q3 总 3 句（报名截止/6月24日/6月10日）、Q5 总 4 句（3月3日/3月15日/3月20日/台账）。

| 模式 | Q3 检索级 | Q5 检索级 | 说明 |
|---|---|---|---|
| vector（基线） | 2/3 | 4/4 | 缺"报名截止"字面（片段截断 800 字） |
| hybrid | 3/3 | 4/4 | 两路融合，Q3 补上字面命中 |
| hybrid_rerank（N=8） | 3/3 | 0/4 | 重排截断到 8 条，挤掉补块里的关键句（模型首跑 mmarco 镜像 404 → 换 bge-reranker-base） |
| hybrid_rerank（N=16） | 3/3 | 4/4 | 重排只排序不牺牲召回，推荐配置 |

**BM25 独立价值（探测证据）**：对 8 条关键词查询，BM25 均引入了纯向量 top8 漏掉的来源，例：
- 「转专业」→ 向量漏、BM25 引入《智能工程学院2025级普通本科生转专业工作实施细则》
- 「寒假」→ BM25 引入《关于做好2026年寒假假期学生教育管理工作的通知》

**结论**：
1. 对本基准 6 题，关键句命中已被同文档补块覆盖，三档（vector/hybrid/rerank-N16）Q3/Q5 检索级命中均接近饱和；hybrid 补上了 vector 在 Q3 缺的"报名截止"字面。
2. 混合检索的增量价值在**向量初始召回漏掉的文档**上（关键词精确命中、语义重叠低时更鲁棒）——论文应强调的定位。
3. 重排 N=8 截断会伤宽泛题召回，N=16 恢复——默认 RERANK_TOP_N 已设为 16。

---

## 2026-08-08 ｜ 批次 3：全量库分块消融 + 三个数据 bug 修复

### 11. rebuild 覆盖 bug（块 ID 撞车）与 source 撞车
- **改了什么**：`rebuild_kb.py` 块 ID 从 `sha256(标题)` 改为 `sha256(相对文件路径)`；source 标签从「文件名 stem」改为「相对路径（分类/文件名）」。
- **为什么**：跨分类同名文档（65 组 / 141 篇）标题相同 → 块 ID 撞车被 Chroma 后写覆盖，整篇文档丢失（块数 6432→5507 的元凶）；source 用 stem 则补块时同名文档被当成一篇。
- **效果**：全量 1194 篇全部入库（300/50 重建 5919 块）；Q3 检索遗漏（历史 0 分）彻底修复——全档 Q3 关键句 3/3 命中。

### 12. chroma_db 损坏定位与修复
- **现象**：500-70 档 eval segfault 后，所有 rebuild 都在 `chromadb.api.rust._count()` 崩（Windows access violation），日志在模型加载后中断。
- **排查**：`python -X faulthandler` 定位到 `_count()`；全新临时库 count 正常、现有库 count 崩 → 现有库 sqlite/HNSW 索引损坏。
- **修复**：删除 chroma_db 重建（源头 scraped_docs 完好，库为可再生派生数据）。
- **教训**：`| tail` 会掩盖 segfault 退出码（假 exit=0），排查崩溃须直接取 python 退出码或用 faulthandler。

### 13. 嵌入模型离线加载
- **改了什么**：消融脚本加 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`。
- **为什么**：sentence-transformers 每次加载模型都发 hf-mirror.com HTTP 校验，网络异常时加载挂起/segfault。
- **效果**：模型加载 <3 秒，离线稳定。

### 14. 全量库分块参数消融（三阶段，1195 篇全量，修复版重建）
| 阶段 | 范围 | 方法 | 结论 |
|---|---|---|---|
| 1 粗网格 | 12 组合（块 200-500 × 重叠 30-70） | 检索级关键句命中 | Q3 全档 3/3；Q5 台账由重叠主导（50/70 命中、30 缺）；probe 5/6 平 |
| 2 细网格 | 7 组合（300-50 → 500-60） | 完整 6 题链路 + 关键句 | 答案质量全档满分，分块参数不再是瓶颈 |
| 3 top_k | 5/8/12/16 | 完整 6 题链路 | 8 最优，5 有损（Q2 丢 4/17），12/16 无增益 |

**打分验收（DeepSeek + 同一评分规则，60 分制）**：300-50 首次 54/60（Q4 4 分：LLM 偶发把 2025 年三下乡安排当答案，个人申报 6月30日 应为 2026 年 7月10日）→ 重跑 **60/60**；300-70、350-60 均 **60/60**。

**最终参数：CHUNK_SIZE=300 / OVERLAP=50 / TOP_K=8**（保持默认，与 10 文档消融一致，全量验证满分）。

**已知隐患（不排期）**：Q4「最近有安排暑期社会实践」query 隐含年份倾向，检索器不敏感，2025 年通知常排在 2026 年前（300/50 稳定如此），LLM 偶发用错年份。
**2026-09-17 评估后决定不做**，理由三条：①显式年份已可用（`_extract_year` + Chroma `where={"year": year}` 的 ±1 年窗口），缺的只是隐性时效词（「最近」「今年」）；②失败形态是"偶发用错年份"的质量瑕疵，不是 Q6 那种归属错误，而它要改**检索排序**，得重跑消融、可能拖累基准；③文档原先写的方案「加权/过滤最新年份」本身不成熟——「去年的心理健康工作」就该往旧了查，无脑提权最新年是错的。真要做，得先做时间词识别（最近/今年/最新 ↔ 去年/往届），那是**新功能**不是修 bug。

### 15. 召回自检 —— LangGraph 状态机 + 低置信度拒答（2026-09-17）

- **测试**：新增 `tests/test_recall_guard.py`（30 条：节点 / 条件边 / 环 / 终止性 / 判官故障兜底，DI 假件、零网络）+ `tests/test_hybrid_retriever.py`（9 条：RRF 融合保留 `vector_distance`，含「BM25 第一名污染 distance」这条机理）。全套 **96 passed**。
- **改了什么**：
  - 新增 `src/recall_guard.py`：LangGraph `StateGraph`，四节点 `retrieve → grade → format|refuse`，条件边 + **改写重检环**。
  - `_search_knowledge_base` 拆成 `_retrieve_candidates`（返回**信封** `{candidates, short_circuit, timings}`）+ `_format_context` + `_search_plain`，公开同步 API 不变。
  - `rrf_fuse` 融合时**另存** `vector_distance`（保留向量路原始余弦距离）。
  - `_call_llm` 加 `system/tools/max_tokens/temperature` 覆盖口（默认值全保持原状，主循环零变化）。
  - `_get_vector_store` 补双检锁（原先无锁，FastAPI 与 MCP 同进程两条线程并发首访会双建）。
  - 开关 `RECALL_GUARD`（**默认 off**，保持改造前行为）+ `RECALL_GUARD_MAX_ATTEMPTS`（默认 1 = 环关）。
- **为什么**：**低置信度拒答**是真功能缺口。改造前 `distance_threshold=0.85` 赋值后**从未被读过**（`vector_store.py` 的 `search_similar` 不引用它）；唯一能拒答的 `_refuse_out_of_kb_school` 只做**校名**校验，问「河南工学院食堂几点开门」这种**本校但库里没有**的题，会把最不相关的块塞给 LLM 让它编。补这个缺口需要条件分支 + 环，这正是状态机相对 if/else 的价值所在。
- **阈值必须从真实库标定**（`my_rag_collection` 实测向量余弦距离）：

  | query | 最佳距离 |
  |---|---|
  | Q5 心理健康 / Q3 报名截止 / Q4 暑期实践 / Q1 端午 / Q2 运动会 | 0.143 / 0.203 / 0.249 / 0.255 / 0.307 |
  | 转专业（BM25 独立价值案例） | 0.384 |
  | 河南工学院食堂几点开门 | 0.465 |
  | 今天天气怎么样 / 推荐几部电影 | 0.603 / 0.613 |

  动态范围只有 0.14~0.61 —— 那个 `0.85` **永远不可能触发**。故取 `PASS_DIST=0.30`（基线 5 题全部 ≤0.307 → 基本免检）。**换 embedding 模型后必须重新标定**。
- **两个关键实现决定**：
  1. **没有一个跨检索来源统一的相似度分数可以卡阈值**。融合后 `distance` 可能是 BM25 的归一化分（第一名恒为 0.0 → 显示"相似度 100%"），也可能是补块继承来的。唯一可信的是向量路原始余弦距离，由 `rrf_fuse` 单独留在 `vector_distance` 字段。**"筛 origin=='vector' 再取距离"会拿到空集**——RRF 保留 distance 更小的那份，而 BM25 第一名恒为 0.0，几乎总赢。
  2. **只有「快通过」没有「快拒答」**：距离低 ≠ 答案在库里（`转专业` 距离 0.384 看着相关，正确文档却不在向量 top8）。所以快筛**只能证伪、不能证真**——单边放行安全，单边拒答会误杀这类靠 BM25 捞回来的题。
- **效果**（`eval_guard_probe.py`，真实检索链路，硬负例 6 + 无关题 4 + 正例 5）：

  | 配置 | 硬负例拒答 | 无关题拒答 | 正例放行 | 耗时（负例/正例） |
  |---|---|---|---|---|
  | `RECALL_GUARD=off`（= 改造前） | **0/6** | 0/4 | 5/5 | 0.26s / 0.27s |
  | `RECALL_GUARD=on` | **6/6** | 4/4 | 5/5 | 1.04s / 0.45s |

  改造前 6 条硬负例全部放行，每条往 LLM 上下文里塞 6.2k~8.8k 字符的无关内容；开启后全部拒答，正例**零误拒**，且正例耗时只涨约 0.2s（距离够近走快通过通道，不调判官）。
- **消融：环不开**（`RECALL_GUARD_MAX_ATTEMPTS` 1 vs 2）。干净负例上环开环关**打平**（都 6/6、4/4），环**没带来任何收益**；但在灰区题「招生办咨询电话」上环有害——环关正确拒答，环开翻成放行（**两次独立运行复现**）。机理：判官第二轮**不记得自己已经判过不充分**，改写重检后对着同样无关的新片段重新判、翻成 sufficient，**把一次正确的拒答变成了放行**。无收益 + 有反效果 → 默认关环，代码保留可开关（消融要能复跑）。
- **已知边界**：判官判的是**相关性**而不是**可答性**，所以「主题词在库、具体事实不在库」的灰区题（如「现任校长是谁」，库里只有外单位领导的人名）会被放行。这不是阈值能修的——要么改判官提问方式（问"片段能否支撑回答"而非"是否相关"），要么走 tool_use 结构化输出。另一条路是让判官带上「上一轮已判不充分」的上下文后重开环。
- **⚠️ 顺带发现：6 题基准的 60/60 有 ±10 分采样噪声。** 为验证「重构是否改变行为」用 **HEAD 原码**跑了两遍：**60/60 与 50/60**（同一份代码！）。我改后的 guard=off 两遍则是 50/60、57/60。根因是 Q6 这类题要过**两级 LLM**（回答 + 打分）：某次 LLM 把工具参数写成 `query='招生录取分数'`——**丢了校名**，库外闸门没东西可拦，于是返回 20 块河南工学院分数，回答里如实列了 **52 个**三位数字，评分 LLM 按「含基准表外数字即判幻觉」给了 0。检索层两次完全一致，差别只在**模型最后那段回答要不要多嘴抄数字**。**结论：60/60 是单次采样、不是稳定性质；自检层的效果必须用检索层的确定性指标量（本条目这张表），不能用 60/60 量。**

### 16. 首句净化 —— 扣住工具调用前的英文旁白（2026-09-17）

- **改了什么**：
  - 新增 `src/stream_gate.py`：`OpeningGate`，每轮扣住流开头，直到出现**第一个汉字**。
  - `app_backend.run_stream` 的流式排空循环接上闸门：`gate.feed(delta)` 决定下发什么，轮末 `gate.finish(had_tool_use=...)` 收尾。
  - `SYSTEM_PROMPT` 加第 16 条：调用工具前不输出任何文字说明、全流程只用中文。
- **为什么**：回答的第一句是英文旁白——

  > `I'll search the knowledge base for information about the 2026 Dragon Boat Festival holiday根据知识库中的通知，河南工学院…`

  模型在发起工具调用前先吐一句英文说明，而 SSE 是**逐 delta 直发**的，等这一轮结束、看到 `tool_use` 时英文早已显示在用户屏幕上。**这一轮的文本从头到尾都不属于回答**：`run_stream` 往 `messages` 里只 append 了 tool_use 块，**文本块被丢弃**——它本来就没被当成答案，却被实时播了出去。
- **是既有缺陷，不是改造引入的**：用 HEAD 原码复跑确认，6 题基准里 **4~5 题**首句都是这个旁白。

  | | prerf(HEAD) | prerf2(HEAD) | 本次改造 guard=off | 本次改造 guard=on |
  |---|---|---|---|---|
  | 出现题数 | 4/6 | 4/6 | 5/6 | 3/6 |

- **为什么用「汉字」当放行信号**：`tool_use` 块在流里**排在文本块之后**才到达，等它到就已经太晚了；而本轮是纯文本轮还是工具轮，要等整轮流完才知道。**「汉字」是唯一在流开头就能拿到、且能区分旁白与正文的信号**（SYSTEM_PROMPT 规则 9 要求用中文回复，正文必然含汉字）。
- **行为**：
  - 出现汉字 → 扣住的英文前缀丢弃，从汉字起原样放行。正文以汉字开头 → **流式几乎无延迟**（第一个 token 就出字）。
  - 整轮无汉字 + 有 `tool_use` → 扣住的就是旁白，丢弃。
  - 整轮无汉字 + 无 `tool_use` → 是一段正常的纯英文/数字回答，**整段补发**（不能把英文提问的回答吃掉）。
- **测试**：`tests/test_stream_gate.py` 10 条（纯字符串，零依赖），含「逐字喂与整段喂结果一致」（切片边界不影响结果）。全套 **106 passed**。
- **已知边界**：闸门只认「汉字」，英文旁白后若**同轮**又跟着中文旁白（如 `I'll search… 让我查一下知识库。`）再调工具，那段中文旁白会在开闸瞬间放行、漏到前端。实测判官的旁白都是纯英文，未遇到此情况；真要根治得让模型（规则 16）或改为延迟整轮下发。

### 17. 库外主体闸门二次校验 —— 堵住「LLM 改写掉校名」的绕过路径（2026-09-17）

- **改了什么**：
  - 新增 `_guard_tool_call(name, user_question)`：工具调用前用**用户原话**再过一次库外主体校验，命中就不跑检索。
  - `AgentLoop.run_stream` 与 `AgentLoop.run` 两处工具调用点都接上（MCP 路径不传原话，行为不变）。
  - 顺带修一个**既有的可观测性缺口**：工具返回后的 `add_step(...)` 只 append 进 `thinking_steps`、**没有 `yield ThinkingEvent(...)`**，所以这些步骤在流式过程中客户端根本看不到（只在最后随 `DoneEvent` 一次性下发），评测回放里也查不出走了哪个分支。非流式的 `run()` 是会下发的，两处不一致。已统一补上 `yield`。
- **为什么**：Q6「河北工学院的录取分数线是多少？」实测同一份代码跑两遍得到 **60/60 与 50/60**。根因是这道题要过**两级 LLM**，回答那级某次把工具参数写成 `query='招生录取分数'`——**把校名改写掉了**。检索层那道闸门（`_retrieve_candidates` 里的 `_refuse_out_of_kb_school`）只看得到**工具参数**，于是没东西可拦，检索返回本校（河南工学院）分数，回答如实列进 52 个三位数字，评分 LLM 按「含基准表外数字即判幻觉」给了 0。
- **为什么用用户原话**：「问的是不是外校」是**用户问题**的性质，不是工具参数的性质。工具参数是 LLM 改写过的中间产物，随时可能把关键词洗掉；用户原话里校名跑不掉。
- **不会误伤**：`_refuse_out_of_kb_school` 只要在 query 里看到主体校名就早退放行，所以比较类问句「河南工学院和河北工学院哪个好」照常放行（已测）。只对 `search_knowledge_base` 生效，时间/计算/天气不受影响。
- **测试**：`tests/test_out_of_kb_guard.py`（8 条，纯字符串，钉住两层闸门的分工）+ `tests/test_q6_rewrite_integration.py`（4 条，**假 LLM 端到端**复现「LLM 把 query 改写成 `招生录取分数`」这条路径，验证闸门真的接在 `run_stream` 上而不是死代码）。全套 **118 passed**。
- **做过变异验证**：临时把 `_guard_tool_call` 改成恒返回 None（模拟修复不存在），4 条集成测试里 **3 条失败**（第 4 条「正常问题照常检索」两边都该过）——确认测试真的钉住了这次修复，不是碰巧通过。
- **效果**：连跑 3 次 6 题基准，Q6 全部 10/10、总分 60/60。**注意这不能证明修复有效**——这几次 LLM 未必会改写掉校名，60/60 本身又有 ±10 采样噪声。真正的证据是上一条那个假 LLM 的端到端测试（把改写钉死，不依赖模型随机性）。

---

### 18. 知识库污染的清出 + 「上传可见 / 可撤销」改造（2026-09-20）

- **发现的真问题**：线上 `my_rag_collection` 是 **6297 块 / 1199 source**，而所有文档与全部评测成绩（60/60、硬负例 0/6→6/6）说的都是 **5919 块 / 1194 篇**。
  - 根因：**5 篇测试期间走 `/upload` 传进来的文档**（共 378 块）混在库里躺了半个月。其中 `太原科技大学2026年硕士研究生招生拟录取名单（二）.pdf` 一篇就占 **290 块**——既是**外校**内容，又含**大量真人姓名**；还有一篇真人课程论文（学生姓名在文件名里）。
  - 这是**项目里最严重的数据事故**：线上系统与所有被评测的系统**不是同一个**。当时测试的两次问答（端午节 / 寒假）都可能命中这 378 块里任何一块。
  - 该类目内计数恰好是 5919，与 `改进记录` 第 14 条「全量 1194 篇入库（300/50 重建 5919 块）」**一字不差**，交叉验证了真值。
- **为什么半个月没人发现**（这才是要改的东西）：
  1. `get_knowledge_base_stats` 的 `total_chunks = collection.count()` 是**整个 collection**，而 `category_counts` 只遍历 13 个硬编码爬虫分类——上传的块打的是 `category="用户上传"`，不在其中。**两个数对不上，却没有任何地方把这个差额说出来**。
  2. 入库之后**没有任何 API 能撤销**。传错一个文件，它就在库里永久生效，只能开 Python 手敲 `col.delete()`（当时就是这么清的）。
- **改了什么**：
  - 新增 `UPLOAD_CATEGORY` 常量；`ingest_uploaded_files` 与统计共用它，标签不再靠魔法字符串。
  - `KnowledgeBaseStats` 增 `uploaded_chunks` / `unaccounted_chunks`；`/kb-stats` 一并返回。**`unaccounted_chunks` = 总数 − 13 分类之和 − 上传数**，正常恒为 0，非 0 就是"库里有来源不明的内容"。它比"专门盯上传"更强：不管污染打的是什么标签都躲不过。
  - 新增 `remove_uploaded_file()` + `DELETE /upload/{文件名}`（受保护）+ `app_backend.delete_uploaded_file()`（返回 `status` 供路由分流 ok / not_found / rejected / failed）。
  - **安全约束**：只删 `category == UPLOAD_CATEGORY` 的块，爬虫语料一律 403 拒删。`chroma_db` 没有备份，一个 DELETE 请求不该有能力抹掉官网语料。
  - `rebuild_kb.py` 抽出 `corpus_txt_files()` 并**排除 `_uploads/`**：原本的 `rglob("*.txt")` 会把上传的 `.txt`/`.md` 当成"官网文章"重建进库，**直接污染消融实验的数字且完全看不出来**。
  - 清出 5 篇污染文档（378 块）→ 恢复 5919 块 / 1194 篇；同时删掉 `scraped_docs/_uploads/` 下 5 个残留文件。
- **踩到并修掉的一个自己写的 bug**：路由最初用 `{filename}` 默认路径转换器，而**线上 1194 篇语料的 source 全部带斜杠**（`分类/标题.txt`）——默认转换器不跨 `/`，请求会先 404，那道 403 安全闸门**永远够不着、成了摆设**。改用 `{filename:path}`。
  - 第一次的校验写法 `Path(name).name != name` 也是错的：它把带斜杠的合法语料名全判成"非法"，**把 403 换成了 400**，问题只是换了个马甲。最终抽成 `is_valid_source_name()`：放行带斜杠的 source，只拒空串 / 绝对路径 / 反斜杠 / 含 `..`。有参数化测试 + 一条"拿线上真 source 全跑一遍"的测试钉住。
- **测试**：新增 `tests/test_upload_lifecycle.py`（29 条，真实临时 Chroma）。全套 **118 → 147 passed**。
- **端到端验收**（起真服务跑的，19/19 通过）：基线 `uploaded=0 / unaccounted=0` → 上传 1 篇 → `total 5919→5920, uploaded=1` → 问"2027 年春季学期选课什么时候开始"**答出上传文档里的 12月8日**（证明真进了检索）→ 同名重传不叠加 → `DELETE` 删 1 块 → 回到 5919 → 重复删 404 / 爬虫语料 403 / 路径穿越 400 / 无 key 401。

### 19. 两处"import 就产生副作用"的隐患（2026-09-20）

- **改了什么**：
  - 6 个脚本（`rebuild_kb` / `crawl_ablation` / `eval_baseline` / `eval_guard_probe` / `eval_score`）的 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, ...)` 与 `logging.basicConfig(...)` 从模块级移进 `if __name__ == "__main__":`。
  - `_cache_set` 的 `redis.setex(key, ttl, val)` → `redis.set(key, val, ex=ttl)`（redis-py 7.x 已把 `setex` 标 deprecated，自 2.6.12 起）。
- **为什么**（stdout 那条）：**任何 import 这些脚本的人，stdout 都会被悄悄换掉**。这不是理论风险——本次新增测试 import `rebuild_kb` 取 `corpus_txt_files`，pytest 立刻炸在 `ValueError: I/O operation on closed file`，**整套测试报告都出不来**（捕获流被 wrapper 顶掉，teardown 时原流已关）。与第 17 条之前删掉的 `src/logger.py` 属同一类缺陷。
  - 用 `__main__` 而不是挪进 `main()`：这些行在 `eval_baseline.py` 里位于 argparse/env 设置与 `from app_backend import ...` **之间**，包进 `__main__` 可保证脚本执行时序与原来**逐行一致**。
  - `app_fastapi.py` 与 `mcp_server/server.py` 的模块级 `basicConfig` **保持不动**：它们是被 uvicorn / MCP 以**模块**身份导入的，加 `__main__` 守卫会直接干掉它们的日志配置。
- **为什么**（setex 那条）：现在只是 DeprecationWarning，将来 redis-py 升大版本会真炸。
- **效果**：测试报告恢复正常；服务器日志中该警告消失。缓存写入验证：TTL 实测 **3587 秒**（3600 减去已过时间），`ex=` 确实生效、key 会自动过期；同一问题第二次问命中缓存（text 帧 238 → 15，首步 `⚡ 命中缓存`）。
- **顺带确认没改坏**：`eval_guard_probe.py` 复跑，行为与文档一致（guard off → 硬负例 0/6 拒答、正例 5/5 放行）——第 19 条对评测脚本**行为中性**。

### 20. Agent 主循环改造成 LangGraph 状态机（2026-09-22）

- **改了什么**：
  - 新增 `src/agent_graph.py`：Agent 主循环的 `StateGraph`，五节点 `prepare → llm → tools → finalize|exhausted`，`tools → llm` 是那个环。
  - `app_backend.py` 新增开关 `AGENT_GRAPH`（**默认 off**）；`AgentLoop.run_stream` 拆成分派器 + `_run_stream_graph` + `_run_stream_legacy`（原实现原样保留，也是新路径的降级目标）。另新增 `_classify_refusal`（前缀 → 结构化拒答原因）与 `_to_agent_event`（dict payload → 事件对象）。
  - 新增两个测试文件：`tests/test_agent_graph.py`（31 条）、`tests/test_agent_graph_equivalence.py`（12 条）。
- **为什么**：`for rnd in range(MAX_ROUNDS)` 手写的 ReAct 循环**本身就是一张图**。理由与第 15 条同源——**分支与环显式化**。工具的库外二次校验、两种拒答的分类、轮数终止，都从循环体里的 if 变成图上的节点与边；再往主循环里加环节（更多工具路由、多轮改写、并行工具）不必继续往 `run_stream` 里塞分支。`AGENT_GRAPH` 沿用 `RECALL_GUARD` 的策略：**新路径先并存、默认关、验证过了再谈打开**。
- **几个刻意的设计决定**（都有注释，写在这里备查）：
  - **`messages` 不用 `add_messages` reducer**。里面装的是 Anthropic 原生块格式（`{"type":"tool_use"}` / `tool_result`），`add_messages` 会把 dict 转成 LangChain Message 并改写 content 结构，破坏请求体。改用覆盖语义、节点自己拼列表——与 `RecallState` 里"计数器覆盖、历史累加"的取舍同源。
  - **两个判定分在两条边上**：`llm` 之后问"LLM 要工具吗"，`tools` 之后问"轮数用尽吗"。合在 `llm` 之后会把最后一轮的工具执行跳掉——原实现 `range(max_rounds)` 最后一轮**仍然执行工具**再兜底，那是个静默行为差异。
  - **工具执行必须保持 `asyncio.to_thread`**。工作线程里没有 running loop，`_search_knowledge_base` 的同步桥才会走安全的 `asyncio.run` 分支；改成直接 `await` 会落到 `ThreadPoolExecutor` 降级分支并阻塞事件循环数秒。
  - **`recursion_limit` 由 `round` 计数器推导（`4n+6`），不拍死数** —— 第 15 条那个坑（1.2.10 默认 10007，且死数会先于计数器触发 `GraphRecursionError`）。
  - **条件边返回值全是字面量、穷尽 path_map**（`_branch.py` 是 `self.ends[r]`，未映射 key 直接 KeyError，而 SSE 侧没有兜住一切的 try/except）。
  - **依赖一律 lambda 晚绑定**：图缓存在实例上只构建一次，早绑定会让之后对 `execute_tool` 等的 monkeypatch 失效，与手写路径（每次现取模块全局）行为不一致。
  - **图按 `AgentLoop` 实例缓存，不做模块级单例**：`api_key` 来自请求头，图构建时就要绑定，没法像 `_recall_graph` 那样全局复用。
- **验证**：
  - **190 条测试全绿**（147 条原有 + 43 条新增）。
  - **等价性测试**是这次改造的主要安全网：同一条对话、同一个假 LLM、同一个假工具，`AGENT_GRAPH=off/on` 各跑一遍，断言**事件序列、发给 LLM 的 messages、工具调用参数逐项相等**（5 个场景：工具轮+回答轮 / 纯回答 / 非流式补发 / 单轮多工具 / 轮数用尽）。图重写最容易出的错不是崩溃，而是"少发一个思考步骤""多跑一轮工具"这种**不会让任何单测变红**的静默漂移。
  - `eval_guard_probe.py` 复跑与文档一致（guard off → 硬负例 0/6、无关题 0/4、正例 5/5），**检索层行为中性**。
  - **真实端到端冒烟**（`AGENT_GRAPH=on` + `RECALL_GUARD=on`，真 LLM + 真检索 + 嵌套自检图）：正例题答案正确（运动会 2026-04-16~17）、负例题正确拒答。
- **一个查实后排除的疑点**：冒烟时观察到负例题在 `on` 下调了 **2 次**检索而 `off` 只调 1 次。定向排查确认两次的 query **不同**（`食堂 开门 营业时间` → `餐厅 就餐时间 供餐时间`），是模型收到拒答后换个问法重试，属 `TEMPERATURE=0.3` 的采样差异，**不是图重复执行同一个 tool_use**（无重复 id）。结构一致性由等价性测试保证，真机跑的是冒烟、不是逐字节对比。
- **已知边界**：
  - 默认 `off`，新路径的实际收益（可维护性）尚未在真实流量上检验过；打开前建议先用 `AGENT_GRAPH=on RECALL_GUARD=on python eval_guard_probe.py --answers` 跑一轮对照。
  - 图路径的异常回退窗口很窄：首个事件在 `prepare` 节点就发出，一旦开始吐事件就不再回退（避免重复输出），只兜"图根本没跑起来"（构建失败 / langgraph 缺依赖）这一类。

---

## 评测数据说明（2026-08-08 清理 → 2026-09-16 部分恢复）

2026-08-08 清理时删除了评测脚本与全部结果文件（`eval_results_*.json` / `eval_chunk_grid/` / `eval_chunk_full/` / `qwen_score_prompt_*.md` / `eval_score_*.md`）。其中 **6 题基准那套已恢复**：

| 文件 | 作用 |
|------|------|
| `rag_chat/eval_baseline.py` | 跑固定 6 题（真实链路：混合检索 + LLM），落盘回答与来源 |
| `rag_chat/eval_score.py` | 按基准事实表 + 60 分制规则逐题打分（事实表与规则逐字取自 `eval_score.pyc` 常量表） |

复跑命令与结果：`python eval_baseline.py --tag topk8` → `python eval_score.py eval_results_baseline_topk8.json`，2026-09-16 复跑 **60/60**。**`eval_hybrid.py`（检索级消融脚本）与 25/50/100 题题库仍未恢复**——后者从未入库。

关键结论存档：

- 混合检索消融三档（6 题，60 分制）：`vector` / `hybrid` / `hybrid+rerank`，混合检索落地后 **60/60**。
- 随机评测成绩：25 题 **96%**、50 题 **100%**、100 题回归后 **95%**（见 [CLAUDE.md](../../CLAUDE.md)）。
- 随机 100 题 8 个真错中 6 题修复成功，残 5 题按用户要求舍弃。
