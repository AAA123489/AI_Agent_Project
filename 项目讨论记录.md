# 项目讨论记录

> 2026-07-25，关于项目方向、简历准备、MCP 工具化的完整讨论。

---

## 背景

- 用户正在准备 9 月份 AI 智能体应用层实习
- 已完成一个手搓的 FastAPI + Chroma + Redis RAG 知识库问答系统
- 同学（上半年找到新乡 AI 智能体工作）给了建议

---

## 同学建议

1. **Python**：基础了解即可，没有"学完"这个概念
2. **FastAPI**：后端学一个就够了
3. **概念了解**：Prompt、Skills、MCP、RAG
4. **重点会用**：Claude Code、Codex 等 Agent 产品
5. **Git**：版本控制

---

## 项目评估结论

**可以写进简历，但需要打磨。** 项目底子是中等偏上水平：

### 亮点
- FastAPI + SSE 流式输出，双层 finally + CancelledError 断连兜底
- Chroma 向量检索 + distance 阈值 + 来源多样性去重
- Redis 短期记忆管道（LPUSH + EXPIRE 30min）
- 自研递归文本切分器
- 完整的 README 文档
- Git 提交记录清晰

### 缺口（已部分补齐）
- ❌ 没有测试代码 → 排入第 5 天
- ❌ 没有 Docker / 部署
- ❌ 没有用到 Agent 框架 / MCP → **第 3 天已补齐 MCP Server**
- ❌ 数据库操作用的是同步 Session

---

## 关键决策：第一个项目要不要用 LangChain/LlamaIndex？

**不用。** 当前项目纯手搓是优势，面试能讲清楚原理。

LangChain 和 LlamaIndex 当前代码里都没有用到，`requirements.txt` 里也没有。

---

## 第二个项目方向

讨论后决定：**第一个项目 + MCP Server 就够**，不需要另起一个项目。

把 RAG 系统做成"独立服务 + MCP 工具"双形态：
- FastAPI 后端 → 独立运行，Web 聊天
- MCP Server → 被 Claude Code 调用，Agent 工具

---

## MCP Server 架构

```
Claude Code
    │ stdio (JSON-RPC)
    ▼
mcp_server/server.py        ← MCP 协议层
    │
    ▼
mcp_server/tools.py         ← 3 个工具函数
    │
    ├── VectorStore.search_similar()      ← 现成
    └── rag_pipeline.ingest_document()    ← 现成（asyncio.to_thread 包一层）
```

### 暴露的 3 个工具

| 工具 | 功能 | 实测 |
|------|------|------|
| `search_knowledge_base` | 向量检索知识库 | ✅ |
| `ingest_file` | 文档入库（TXT/MD/PDF） | ✅ |
| `list_sources` | 列出知识库文档来源 | ✅ |

### 新增文件
```
mcp_server/
├── __init__.py
├── tools.py           # 工具实现
└── server.py          # MCP 协议入口
.mcp.json              # Claude Code MCP 配置
```

---

## 面试话术（核心）

> "我手搓了一个 RAG 系统，然后用 MCP 协议把它包装成了 AI Agent 可调用的工具——Claude Code 能直接搜索我的知识库、入库文档。Agent 是大脑，MCP 是手脚。"

简历关键词：FastAPI、Chroma、Redis、SSE 流式、MCP Server、Claude Code

---

## 后续计划（7天冲刺，当前进度：第 3 天完成）

| 天 | 任务 | 状态 |
|----|------|------|
| 1 | 重写 README | ✅ |
| 2 | 聊天前端页面 | ✅ |
| 3 | MCP Server（RAG 工具化） | ✅ |
| 4 | Claude Code Skill + Agent 产品理解 | ⬜ |
| 5 | 补测试 + 项目亮点总结 | ⬜ |
| 6 | 推 GitHub + 简历项目描述 | ⬜ |
| 7 | 模拟面试练习 | ⬜ |
