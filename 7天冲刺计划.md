# 7 天冲刺计划 —— 从项目完成到简历可投

> **背景：** 18 天从零搭建了 FastAPI + Chroma + Redis + RAG 完整闭环。接下来 7 天不做新功能，专注包装、演示、面试准备。
>
> **目标：** 9 月份 AI 智能体应用层实习，简历有东西可写，面试有东西可讲。

---

## 第 1 天：重写 README，让项目"会说人话"

**目标：** 现在 README 只描述了「SSE 流式对话服务」，但实际做的远不止这些。今天改到"陌生人 clone 下来能看懂这是什么、怎么跑、厉害在哪"的程度。

### 上午（1.5 小时）：写「项目简介」+「核心能力」

打开 [README.md](README.md)，把开头的「项目简介」改成下面这样（可以自己改述，但要点要全）：

```markdown
# AI 智能知识库问答系统（RAG Chat）

基于 FastAPI 的 RAG（检索增强生成）智能问答后端，支持文档入库、向量语义检索、
多轮对话记忆、SSE 流式响应，端到端闭环。

## 核心能力

| 能力 | 说明 |
|------|------|
| 📄 文档解析 | 支持 TXT / Markdown / PDF，自动解析并提取文本 |
| ✂️ 智能分块 | 自研递归文本切分器，按段落→句子→空格优先级切分，带重叠窗口 |
| 🔢 向量检索 | Chroma 向量数据库，余弦相似度检索，distance 阈值兜底 |
| 💬 流式对话 | SSE（Server-Sent Events）流式推送，逐字返回 LLM 生成内容 |
| 🧠 多轮记忆 | Redis 短期记忆管道，LPUSH + EXPIRE 30 分钟自动过期 |
| 🔐 API 鉴权 | 自定义 X-API-Key 认证，FastAPI 依赖注入实现 |
| 📊 对话持久化 | SQLAlchemy 异步 ORM，对话历史自动入库、支持查询 |
```

然后把「技术栈」写在简介下面：

```markdown
## 技术栈

- **Web 框架**：FastAPI + Uvicorn（ASGI）
- **数据库**：SQLite + SQLAlchemy 2.0 ORM
- **缓存 / 记忆**：Redis（异步客户端 redis-py）
- **向量库**：Chroma（本地持久化 + HNSW 余弦索引）
- **LLM 调用**：aiohttp 异步流式请求 → DeepSeek API
- **流式协议**：SSE（Server-Sent Events）
- **数据校验**：Pydantic V2
- **日志系统**：RotatingFileHandler，500KB 自动滚动
```

### 下午（1.5 小时）：更新「目录结构」+「快速开始」+ 架构图

**1. 目录结构** —— 把现在的目录树替换成实际的：

```
AI_Agent_Project/
├── main.py                  # FastAPI 应用入口（生命周期、CORS、路由）
├── database.py              # SQLAlchemy 引擎 & 会话工厂
├── model.py                 # ChatHistory ORM 数据模型
├── redis_client.py          # Redis 短期记忆管道
├── prompts.py               # RAG Prompt 模板（历史+上下文+问题）
├── rag_pipeline.py          # RAG 文档入库流水线（解析→切分→Hash→入库）
├── text_splitter.py         # 自研递归文本切分器
├── document_parser.py       # PDF 解析器（pypdf）
├── requirements.txt         # 项目依赖
├── .env.example             # 环境变量模板
├── test_sse.html            # 浏览器 SSE 流式测试页面
├── app/
│   ├── dependencies/__init__.py   # API Key 鉴权 + 数据库会话注入
│   ├── routers/chat.py            # /chat（SSE流式+向量检索）
│   │                              # /history/{user_id}（对话历史）
│   └── schemas/chat.py            # Pydantic 请求/响应模型
└── src/
    ├── config.py             # .env 配置管理
    ├── llm_client.py         # LLM 异步流式调用客户端
    ├── logger.py             # 日志配置
    └── vector_store.py       # Chroma 向量库封装
```

**2. 架构图** —— 在「项目简介」后面加一段：

```
## 系统架构

用户浏览器 / Postman
        │
        ▼
   POST /chat ──────────────────────────────┐
        │                                    │
        ▼                                    │
   X-API-Key 鉴权（依赖注入）                 │
        │                                    │
        ▼                                    │
   ┌─ 向量检索 ─┐   ┌─ Redis ─┐             │
   │ Chroma     │   │ 查询历史 │             │
   │ 语义匹配   │   │ 多轮对话 │             │
   └─────┬──────┘   └───┬─────┘             │
         │               │                   │
         ▼               ▼                   │
   ┌──────────────────────────┐              │
   │  拼装 RAG Prompt         │              │
   │  历史 + 上下文 + 问题     │              │
   └──────────┬───────────────┘              │
              │                              │
              ▼                              │
   ┌──────────────────────────┐              │
   │  DeepSeek API 流式调用    │              │
   │  aiohttp 异步逐块读取     │              │
   └──────────┬───────────────┘              │
              │                              │
              ▼                              │
   ┌──────────────────────────┐              │
   │  SSE StreamingResponse   │──────────────┘
   │  逐字推送给前端           │
   └──────────┬───────────────┘
              │
              ▼
   ┌──────────────────────────┐
   │  Redis 写入最新对话        │
   │  SQLite 持久化历史记录     │
   └──────────────────────────┘
```

**3. 快速开始** —— 加一个 `.env.example` 的说明：

````markdown
### 4. 配置环境变量

复制 `.env.example` 为 `.env`，填入你的 DeepSeek API Key：

```bash
cp .env.example .env
# 然后编辑 .env，把 your_api_key_here 替换成真实的 Key
```
````

### 验证标准

```bash
# 一个完全陌生的同学 clone 你的项目后，能看着 README 跑起来
git clone <你的仓库>
cd AI_Agent_Project
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
cp .env.example .env   # 填 Key
python main.py         # 访问 http://127.0.0.1:8000/docs
```

---

## 第 2 天：做一个能演示的聊天前端页面

**目标：** 现在用 Postman 调接口，面试官不可能看。今天做一个**单文件 HTML**，浏览器打开就能聊天，展示整个后端的能力。

### 具体要做的

在项目根目录新建 `static/chat.html`：

**功能要求：**
1. 输入框 + 发送按钮
2. 对话气泡（用户消息靠右、AI 消息靠左，流式逐字出现）
3. 对话历史保留在页面上（刷新就没了没关系，不用额外做持久化）
4. 支持输入 user_id（模拟多用户）
5. 显示每条回复的来源文档（检索到的 chunk 预览）

**技术要点：**
- 用 `fetch` + `response.body.getReader()` 读 SSE 流
- 用 `EventSource` 不行，因为是 POST 请求
- SSE 返回的是 `data: 你好\n\n` `data: 世界\n\n`，前端要逐条解析

**不需要从零写。** 让 Claude Code 帮你写，但必须读懂下面这几个关键点（面试会问）：

> **面试官：** "前端怎么接 SSE 流的？"
> **你：** "用的是 fetch + ReadableStream reader。因为 SSE 需要 POST 请求体，而浏览器原生 EventSource 只支持 GET，所以我用 fetch 发 POST，然后 `response.body.getReader()` 拿到流，用 TextDecoder 逐块解码，每收到一个 `data:` 行就拼到页面上。客户端断开时 reader 会被 cancel，后端 CancelledError 触发清理。"

### 下午：测试 + 录屏

1. 启动后端 `python main.py`
2. 浏览器打开 `chat.html`
3. 测试场景：
   - 问「FastAPI 有什么特点？」→ 应该检索到文档 + 流式回答
   - 问「你好」→ 验证多轮对话记忆（它应该记得刚才问了什么）
   - 问「今天天气怎么样」→ 应该显示「根据现有资料无法回答」（阈值兜底）
4. 用手机录一段 30 秒的演示视频（横屏，录浏览器全屏），保存为 `demo.mp4` 放项目里（或者传 B 站，README 放链接）

---

## 第 3 天：体验 MCP（重点！面试一定会问）

**目标：** 不需要深挖实现原理，但要做三件事，让自己在面试时有真实体验可讲。

### 上午：理解 MCP 是什么（30 分钟）

花 30 分钟看 MCP 官方介绍：https://modelcontextprotocol.io/introduction

**一句话理解：** MCP 就是 AI Agent 的「USB 接口」——就像 U 盘插电脑不需要装驱动一样，MCP 工具插到 Claude Code 里就能直接用。它是一个标准协议，让不同的工具（文件系统、数据库、API）都以同一种方式被 Agent 调用。

你应该能用自己话说出来：

> **面试官：** "MCP 是什么？"
> **你：** "MCP 全称 Model Context Protocol，是 Anthropic 发布的开放协议。它定义了一套标准——AI 应用怎么去发现、调用外部工具。可以理解为给 AI 装了一个统一的工具接口：不管是查数据库、读文件、调 API，只要实现了 MCP 协议，Agent 就能用同一种方式调用。我用过的场景是给 Claude Code 接 MCP 工具……"

### 下午：动手做两件事

**任务 A：装一个现成的 MCP 工具体验（30 分钟）**

Claude Code 自带 MCP 市场。在 Claude Code 里找到 MCP 面板，安装一个工具（比如文件系统 MCP 或网页抓取 MCP），然后实际用它完成一个任务。

体验完后记下来：
- MCP 工具的配置长什么样（JSON）
- Agent 调用它时你看到了什么（有工具调用的展示）

**任务 B：写一个极简 MCP Server（1 小时）**

在项目里新建 `mcp_demo/server.py`：

```python
"""极简 MCP Server —— 提供「获取当前时间」工具"""
import json
import sys
from datetime import datetime


async def main():
    """读取 stdin 的 JSON-RPC 请求，返回 MCP 工具列表或执行结果。"""
    # MCP 通过 stdin/stdout 与 Claude Code 通信
    for line in sys.stdin:
        request = json.loads(line)
        method = request.get("method", "")

        if method == "tools/list":
            # 告诉 Claude Code：「我有什么工具」
            response = {
                "tools": [
                    {
                        "name": "get_current_time",
                        "description": "获取当前系统时间，支持指定时区（如 Asia/Shanghai）",
                        "inputSchema": {
                            "type": "object",
                            "properties": {
                                "timezone": {
                                    "type": "string",
                                    "description": "时区，默认 Asia/Shanghai"
                                }
                            }
                        }
                    }
                ]
            }
        elif method == "tools/call":
            # 工具被调用
            tool_name = request.get("params", {}).get("name", "")
            arguments = request.get("params", {}).get("arguments", {})

            if tool_name == "get_current_time":
                timezone = arguments.get("timezone", "Asia/Shanghai")
                now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
                response = {
                    "content": [{"type": "text", "text": f"当前时间（{timezone}）：{now}"}]
                }
            else:
                response = {"error": f"未知工具: {tool_name}"}
        else:
            response = {"error": f"未知方法: {method}"}

        # 写入 stdout（MCP 的通信方式）
        sys.stdout.write(json.dumps(response, ensure_ascii=False) + "\n")
        sys.stdout.flush()


if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

**重点不是完整实现，是你能说出：**
- MCP Server 通过 stdin/stdout 和 Agent 通信
- 两个核心方法：`tools/list`（告诉 Agent 有什么工具）、`tools/call`（执行工具）
- 你亲手写了一个极简的 MCP Server，知道它怎么工作的

---

## 第 4 天：写 Claude Code Skill + 理解 Agent 产品

**目标：** 「重要的是会用 Claude Code、Codex 这些 Agent 产品」。光会用不够，要能说出"我自定义过它的行为"。

### 上午：创建项目级 Skill（30 分钟）

在项目根目录 `.claude/skills/` 下新建文件 `code-review.md`：

```markdown
---
name: code-review
description: 审查当前项目的 FastAPI 路由代码，检查错误处理、参数校验、异步是否正确
---

# 代码审查 Skill

你是一个 Python FastAPI 代码审查专家。请按以下清单审查我提供的代码：

## 审查清单

1. **错误处理**：路由中是否有未捕获的异常？finally 块是否正确清理资源？
2. **参数校验**：Pydantic 模型的约束是否合理？Field 的 min_length/max_length 是否恰当？
3. **异步正确性**：async 路由中是否有同步阻塞调用？数据库会话是否正确关闭？
4. **安全性**：是否有硬编码的 Key？CORS 配置是否合理？鉴权依赖是否正确注入？
5. **日志**：关键节点是否有日志记录？

## 输出格式

按严重程度排序（🔴 严重 → 🟡 建议 → 💡 优化），每条附上：
- 文件 + 行号
- 问题描述
- 修复建议（不要直接给代码）
```

然后用一次：在 Claude Code 里输入 `/code-review app/routers/chat.py`

### 下午：理解 Agent 产品对比（30 分钟）

花半小时用 Claude Code 问这些问题，然后用自己的话总结到笔记里：

1. 「Claude Code 和 GitHub Copilot 的核心区别是什么？」
2. 「Claude Code 的 Skill 和 Agent 的 Tool 有什么区别？」
3. 「Codex（OpenAI 的）和 Claude Code 用起来有什么不同？」

面试时你应该能说出：

> **面试官：** "你说你用过 Agent 产品，常用哪些？"
> **你：** "我日常开发用 Claude Code，它是终端里的 AI 编程助手。跟 Copilot 那种补全式的不一样——Copilot 是帮你写完当前行，Claude Code 是你能用自然语言描述一个任务，它会自己去读文件、写代码、跑命令、修 bug。我还给它写了自定义 Skill 来规范代码审查流程，还体验过 MCP 工具扩展。Codex 我也了解，它是 OpenAI 的 Agent 产品，偏云端沙箱执行，跟 Claude Code 的本地终端交互不一样。"

---

## 第 5 天：写项目亮点总结 + 清理细节

**目标：** 简历里只有 3-4 行写项目经历。今天写出这段话 + 确保项目没有遗留问题。

### 上午：写项目亮点（1 小时）

新建 `PROJECT_HIGHLIGHTS.md`（面试前反复看）：

```markdown
# 项目亮点 —— 面试话术

## 一句话描述
基于 FastAPI + Chroma + Redis 的 RAG 智能知识库问答系统，
支持文档解析、向量检索、多轮对话记忆、SSE 流式输出。

## 三个核心亮点

### 1. RAG 检索增强生成闭环
- 手写递归文本切分器：按 \n\n → \n → 。 → 空格 优先级切分，带 overlap 窗口
- SHA-256 Hash 生成唯一 ID：支持幂等入库（同一文档重复入库自动覆盖）
- Chroma 向量检索 + distance 阈值兜底：低于相关性阈值时回复「根据现有资料无法回答」
- 整个管道：文档解析 → 切分 → Embedding → 入库 → 检索 → Prompt 拼装 → LLM 生成

### 2. 流式架构 + 断连防护
- aiohttp 异步逐块读取 LLM 响应 → buffer 拼行 → JSON 过滤 → StreamingResponse
- 双层 finally（LLM 连接层 + 流式生成器层）+ CancelledError 捕获
- 客户端断开时正确关闭上游连接，不泄漏资源

### 3. 多轮对话记忆
- Redis 短期记忆管道：LPUSH 存入 + EXPIRE 30 分钟自动过期
- LRANGE + json.loads 反序列化取历史，拼入 RAG Prompt 上下文
- FastAPI lifespan 管理 Redis 连接池生命周期

## 面试官可能追问的问题 & 我的回答

Q: 为什么用 Chroma 而不是 Faiss / Milvus？
A: Chroma 轻量、本地持久化、Python 原生支持好、不需要额外部署服务。
   对于我这个项目规模足够。如果生产环境数据量大，可以考虑 pgvector 或 Milvus。

Q: Redis 会话过期时间为什么是 30 分钟？
A: 用户对话通常不会跨半小时，30 分钟足够覆盖一次完整的问答交互，
   同时避免内存被长期不活跃的会话占满。

Q: distance 阈值 0.7 是怎么定的？
A: Chroma 使用余弦距离（1 - 余弦相似度）。0.7 意味着相似度需要 > 0.3。
   这个值是调试出来的——太高会漏掉相关内容，太低会召回无关内容。
   实际生产中可以做成可配置的，甚至按文档类型分别设定。

Q: 你这个项目最大的技术难点是什么？
A: SSE 流式管道中断连处理。客户端断开后，服务端的 aiohttp 连接还在，
   如果不处理会导致资源泄漏。我用 CancelledError + 双层 finally 解决的。
```

### 下午：检查清单（30 分钟）

逐项打勾：

- [ ] `.env` 在 `.gitignore` 里 ✓
- [ ] `.env.example` 可以提交 ✓
- [ ] 代码里没硬编码 Key（Key 从 ConfigManager 读）
- [ ] `requirements.txt` 能一键装齐 ✓
- [ ] README 里没有过时的文件路径（检查 `dependencies.py`、`file_handler.py` 这些已经不存在的文件——如果在，删掉）
- [ ] 项目能跑通：`python main.py` → 浏览器开 `test_sse.html` → 能正常对话

---

## 第 6 天：推 GitHub + 写简历里的项目描述

**目标：** 仓库公开、整洁、专业。

### 上午：整理 Git 历史 + 推送（1 小时）

```bash
# 1. 查看当前状态
git status

# 2. 把所有改动加进去
git add .

# 3. 提交（用规范的 commit message）
git commit -m "refactor: 修复 Bug、补全依赖、项目清理

- 修复 parse_pdf 返回值解包不匹配的问题
- 修复 API Key 鉴权中 Request 依赖注入
- 补全 requirements.txt 缺少的核心依赖
- vector_db 改为延迟初始化
- 新增 VectorStore.search_similar_async 异步方法
- 清理无关注释代码和过期测试文件
- 新增 .env.example 环境变量模板
- 删除项目无关的学习资料"

# 4. 推送到 GitHub
git push origin feature/week2-fastapi-init
```

### 下午：写简历里的项目描述（1 小时）

简历项目经历栏，用这个：

```
项目名：AI 智能知识库问答系统（RAG Chat） | 独立开发 | 2026.07

技术栈：FastAPI + Chroma + Redis + SQLAlchemy + DeepSeek API + SSE

项目描述：
基于 RAG 架构的智能问答后端系统，支持多格式文档入库、向量语义检索、
多轮对话记忆与流式响应。实现了从文档解析到 LLM 生成的完整闭环。

核心工作：
· 自研递归文本切分器，支持优先分隔符策略与重叠窗口，保证语义完整性
· 集成 Chroma 向量库实现余弦相似度检索，设计 distance 阈值兜底防止幻觉回答
· 搭建 Redis 短期记忆管道（LPUSH + EXPIRE），实现 30 分钟自动过期的多轮对话上下文
· 实现 SSE 流式响应，双层 finally + CancelledError 确保客户端断连时资源正确释放
· 使用 FastAPI 依赖注入实现 API Key 鉴权，Pydantic V2 完成请求数据校验
· 全链路日志追踪（RotatingFileHandler），异步 ORM 持久化对话历史
```

---

## 第 7 天：模拟面试练习

**目标：** 把「我做的东西」翻译成「我能讲出来的东西」。

### 上午：对着 README + 架构图，用嘴讲一遍（1 小时）

找一面墙（或者对着镜子），讲下面这段话，**录音**，然后自己听一遍：

> 「我做的是一个基于 RAG 架构的智能知识库问答系统。它的核心流程是这样的：用户通过浏览器或 API 发送问题过来，首先经过 API Key 鉴权，然后系统从两个地方获取上下文——一个是 Redis，存了最近 20 条历史对话；另一个是 Chroma 向量库，用问题去检索相关文档块。这三个东西——历史、文档、当前问题——拼成一个 Prompt 发给 DeepSeek，然后用 SSE 流式把回答逐字推回给前端。最后把本轮对话同时存到 Redis 和 SQLite 里。整个过程中，如果向量检索的相关性低于阈值，会直接拒绝回答，避免 LLM 编造内容。」

### 下午：用 Claude Code 做一次模拟面试（1 小时）

在 Claude Code 里发：

```
你现在扮演字节跳动的后端面试官，面试 AI 应用开发实习生岗位。
我说了我做过一个「基于 RAG 的智能知识库问答系统」，
请你针对这个项目提 5 个追问，越细越好。
我回答完一个你再问下一个，每个回答之后给我评价。
```

然后一个一个回答。答不上来的，记下来，去查，去理解。

---

## 这一周结束后的成果

| 产出 | 用途 |
|------|------|
| 完整 README（架构图+能力表+快速开始） | GitHub 门面 |
| `static/chat.html` 前端页面 | 面试演示 |
| `demo.mp4` 演示视频 | 简历附件 / 作品集 |
| `PROJECT_HIGHLIGHTS.md` | 面试前 10 分钟看一遍 |
| `.env.example` | 别人能跑起来 |
| 干净的 Git 历史 | 专业 |
| MCP 体验 + Skill | 面试能聊 Agent 产品 |
| 简历项目描述（6 条核心工作） | 直接复制粘贴 |
| 模拟面试问答录音 | 查漏补缺 |

---

> 💡 **每天做完一项，把进度记在下面。卡住了就问 Claude Code，不要自己在那憋半天。**
>
> 下次对话时把这整个文档发给我，我就知道你在哪一步了。
