# 7 天冲刺计划 —— 从项目完成到简历可投

> **背景：** 18 天从零搭建了 FastAPI + Chroma + Redis + RAG 完整闭环。接下来 7 天做包装 + MCP 工具化 + 面试准备，把项目升级为「RAG 知识库 + MCP Server」双形态。
>
> **目标：** 9 月份 AI 智能体应用层实习，简历有东西可写，面试有东西可讲。

---

## 进度追踪

| 天数 | 任务 | 状态 |
|------|------|------|
| 第 1 天 | 重写 README | ✅ 完成 |
| 第 2 天 | 聊天前端页面 + 录演示视频 | ✅ 完成 |
| 第 3 天 | MCP Server（RAG 工具化） | 🔜 今天 |
| 第 4 天 | Claude Code Skill + Agent 产品理解 | ⬜ |
| 第 5 天 | 补测试 + 项目亮点总结 | ⬜ |
| 第 6 天 | 推 GitHub + 写简历项目描述 | ⬜ |
| 第 7 天 | 模拟面试练习 | ⬜ |

---

## 第 1 天：重写 README，让项目"会说人话" ✅

> **状态：已完成。**

现在 README 已经包含：项目简介、核心能力表、技术栈、系统架构 ASCII 图、完整目录结构、快速开始指南、API 端点文档、鉴权说明、环境变量参考、模块说明、Postman 测试指南。陌生人 clone 下来能看懂、能跑通。

---

## 第 2 天：做一个能演示的聊天前端页面 ✅

> **状态：已完成。**

`static/chat.html` 已做好，功能包括：
1. 对话气泡（用户靠右、AI 靠左，流式逐字出现）
2. 支持输入 user_id（模拟多用户）
3. 显示每条回复的来源文档（检索到的 chunk 预览 + distance）
4. API Key 记忆（localStorage）+ 多会话管理（新建/切换/删除对话）
5. 深色主题，移动端适配

**面试时你能说出的关键点：**
> "用的是 fetch + ReadableStream reader。因为 SSE 需要 POST 请求体，而浏览器原生 EventSource 只支持 GET，所以我用 fetch 发 POST，然后 `response.body.getReader()` 拿到流，用 TextDecoder 逐块解码，每收到一个 `data:` 行就拼到页面上。"

⏸️ **录演示视频暂时跳过**（等 MCP 加完再录，视频内容更丰富）

---

## 第 3 天：MCP Server —— 把 RAG 能力包装成 Agent 工具 🔜

**目标：** 不写玩具 demo，直接把现有 RAG 系统的能力暴露为 MCP 工具——Claude Code 能通过 MCP 协议搜索你的知识库、入库文档。

### 为什么这样做？

原来的计划是写一个极简的"获取当前时间" MCP Server——这个东西太薄了，网上 20 分钟教程就能搭一个。**升级方案：** 把你第一个项目手搓的 RAG 系统，包装成 Agent 可调用的工具集合。

面试时一句话讲清楚：
> "我手搓了一个 RAG 系统，然后用 MCP 协议把它包装成了 AI Agent 可调用的工具——Claude Code 能直接搜我的知识库、入库文档。"

### 上午：理解 MCP 协议 + Python SDK（1 小时）

**MCP 一句话理解：** MCP 是 AI Agent 的「USB 接口」——不同的工具（文件系统、数据库、API）都以同一种标准协议被 Agent 发现和调用。

看一下 MCP Python SDK 的官方 quickstart：
- https://github.com/modelcontextprotocol/python-sdk

核心概念：
- **Server**：你写的 Python 进程，通过 stdio 与 Agent 通信
- **Tool**：Server 暴露的具体能力（比如 `search_knowledge_base`）
- **`tools/list`**：告诉 Agent 你有什么工具
- **`tools/call`**：Agent 调用你的工具

### 下午：动手写 MCP Server（2 小时）

在项目里新建 `mcp_server/` 目录，把现有 RAG 能力暴露为 MCP 工具：

```
mcp_server/
├── __init__.py
├── server.py          # MCP Server 入口（stdio 通信）
└── tools.py           # 工具定义：search / ingest / list
```

**暴露 3 个工具：**

| 工具名 | 功能 | 对应现有代码 |
|--------|------|-------------|
| `search_knowledge_base` | 向量检索知识库，返回相关文档片段 | `VectorStore.search_similar()` |
| `ingest_document` | 把本地文档入库（TXT/MD/PDF） | `rag_pipeline.ingest_document()` |
| `list_knowledge_sources` | 列出知识库中已有的文档来源 | `VectorStore.count_by_source()` |

**技术要点（面试会问）：**
1. MCP Server 通过 **stdio**（标准输入/输出）与 Claude Code 通信，不走 HTTP
2. 使用 `mcp` Python SDK（不是手写 JSON-RPC 解析）
3. Claude Code 的 MCP 配置文件在 `~/.claude/mcp.json` 或项目的 `.mcp.json`

### 配置 Claude Code 连接你的 MCP Server

在项目根目录新建 `.mcp.json`：

```json
{
  "mcpServers": {
    "rag-knowledge-base": {
      "command": "python",
      "args": ["-m", "mcp_server.server"],
      "cwd": "${workspaceFolder}"
    }
  }
}
```

Claude Code 启动后会自动连接，你就能对它说："帮我查一下知识库里关于 FastAPI 的内容"——它会调用你的 `search_knowledge_base` 工具。

### 验证标准

```bash
# 在 Claude Code 里发起对话
> 帮我在知识库里搜索 "FastAPI 有什么特点"

# Claude Code 应该会：
# 1. 展示它调用了 search_knowledge_base 工具
# 2. 返回检索到的文档片段
# 3. 基于这些片段回答你的问题
```

---

## 第 4 天：Claude Code Skill + 理解 Agent 产品

**目标：** 「重要的是会用 Claude Code、Codex 这些 Agent 产品」。光会用不够，要能说出"我自定义过它的行为"。

### 上午：创建项目级 Skill（30 分钟）

在 `.claude/skills/` 下新建 `code-review.md`：

```markdown
---
name: code-review
description: 审查当前项目的 FastAPI 路由代码，检查错误处理、参数校验、异步是否正确
---

# 代码审查 Skill

你是一个 Python FastAPI 代码审查专家。请按以下清单审查我提供的代码：

## 审查清单

1. **错误处理**：路由中是否有未捕获的异常？finally 块是否正确清理资源？
2. **参数校验**：Pydantic 模型的约束是否合理？Field 的 min_length/max_length 是否恰当？
3. **异步正确性**：async 路由中是否有同步阻塞调用？数据库会话是否正确关闭？
4. **安全性**：是否有硬编码的 Key？CORS 配置是否合理？鉴权依赖是否正确注入？
5. **日志**：关键节点是否有日志记录？

## 输出格式

按严重程度排序（🔴 严重 → 🟡 建议 → 💡 优化），每条附上：
- 文件 + 行号
- 问题描述
- 修复建议（不要直接给代码）
```

然后用一次：`/code-review app/routers/chat.py`

### 下午：理解 Agent 产品对比（30 分钟）

花半小时了解这些问题的答案，能用自己的话说出来：

1. Claude Code 和 GitHub Copilot 的核心区别是什么？
2. Claude Code 的 Skill 和 MCP Tool 有什么区别？
3. Claude Code 和 OpenAI Codex CLI 的差异？

**面试时你应该能说出：**

> "我日常开发用 Claude Code，它是终端里的 AI 编程助手。跟 Copilot 补全式的不一样——Claude Code 能用自然语言描述一个任务，自动读文件、写代码、跑命令、修 bug。我还给它写了自定义 Skill 来规范代码审查流程，最关键的是——我把自己的 RAG 知识库做成了 MCP Server，Claude Code 能直接搜索我的知识库、入库文档。这就是 MCP 协议的实际应用：Agent 是大脑，MCP 工具是手脚。"

---

## 第 5 天：补测试 + 项目亮点总结

**目标：** 给核心模块补上单元测试 + 写好面试话术。

### 上午：补测试（1.5 小时）

没有测试代码是简历项目的硬伤。至少补两个：

**`tests/test_text_splitter.py`**（最好写，纯逻辑）：
- 测试空字符串
- 测试短文本（不需要切分）
- 测试长文本正常切分
- 测试 overlap 窗口正确
- 测试中英文混合

**`tests/test_prompts.py`**（纯逻辑）：
- 测试有上下文 + 历史的 prompt
- 测试无上下文的 prompt（纯对话模式）
- 测试空历史的 prompt

```bash
pip install pytest
pytest tests/ -v
```

### 下午：更新项目亮点总结（1 小时）

更新 `PROJECT_HIGHLIGHTS.md`，面试前反复看：

```markdown
# 项目亮点 —— 面试话术

## 一句话描述
基于 FastAPI + Chroma + Redis 的 RAG 智能知识库问答系统，
支持文档解析、向量检索、多轮对话记忆、SSE 流式输出，
并通过 MCP 协议包装为 Agent 可调用的工具集合。

## 四个核心亮点

### 1. RAG 检索增强生成闭环
- 自研递归文本切分器：按 \n\n → \n → 。 → 空格 优先级切分，带 overlap 窗口
- SHA-256 Hash 生成唯一 ID：支持幂等入库（同一文档重复入库自动覆盖）
- Chroma 向量检索 + distance 阈值兜底 + 来源多样性去重
- 完整管道：文档解析 → 切分 → Embedding → 入库 → 检索 → Prompt 拼装 → LLM 生成

### 2. 流式架构 + 断连防护
- aiohttp 异步逐块读取 LLM 响应 → buffer 拼行 → JSON 过滤 → StreamingResponse
- 双层 finally（LLM 连接层 + 流式生成器层）+ CancelledError 捕获
- 客户端断开时正确关闭上游连接，不泄漏资源

### 3. MCP 协议工具化（区别于普通 CRUD 项目的关键亮点）
- 把 RAG 系统的向量检索 + 文档入库能力包装为 MCP Server
- Claude Code 可直接调用 search_knowledge_base / ingest_document 工具
- 体现了「Agent 是大脑，MCP 是手脚」的架构理解

### 4. 多轮对话记忆
- Redis 短期记忆管道：LPUSH 存入 + EXPIRE 30 分钟自动过期
- FastAPI lifespan 管理连接池生命周期

## 面试官可能追问 & 我的回答

Q: MCP 是什么？你在项目里怎么用的？
A: MCP 全称 Model Context Protocol，是 Anthropic 发布的开放协议。
   它定义了一套标准——AI 应用怎么去发现、调用外部工具。
   在我的项目里，我把 RAG 知识库的检索和入库能力封装成了 MCP Server，
   Claude Code 通过 stdio 与它通信，能直接搜索我的知识库、入库文档。
   可以理解为给 AI Agent 装了一个统一的工具接口。

Q: 为什么用 Chroma 而不是 Faiss / Milvus？
A: Chroma 轻量、本地持久化、Python 原生支持好、不需要额外部署服务。
   对于我这个项目规模足够。如果生产环境数据量大，可以考虑 pgvector 或 Milvus。

Q: Redis 会话过期时间为什么是 30 分钟？
A: 用户对话通常不会跨半小时，30 分钟足够覆盖一次完整的问答交互，
   同时避免内存被长期不活跃的会话占满。

Q: 你这个项目最大的技术难点是什么？
A: 两个。一是 SSE 流式管道中断连处理——客户端断开后 aiohttp 连接还在，
   需要 CancelledError + 双层 finally 兜底。二是 MCP Server 的工具设计——
   怎么把已有的 RAG 能力合理地拆成原子工具，让 Agent 能正确选择和组合调用。
```

### 检查清单

- [ ] `.env` 在 `.gitignore` 里
- [ ] `.env.example` 已提交
- [ ] 代码里没硬编码 Key
- [ ] `requirements.txt` 包含 MCP SDK
- [ ] `pytest tests/ -v` 全部通过
- [ ] README 里没有过时的文件路径

---

## 第 6 天：推 GitHub + 写简历项目描述

**目标：** 仓库公开、整洁、专业。

### 上午：整理 Git 历史 + 推送（1 小时）

```bash
git status
git add .
git commit -m "feat: 添加 MCP Server，RAG 能力工具化

- 新增 mcp_server/ 模块，暴露 search_knowledge_base / ingest_document / list_knowledge_sources 三个 MCP 工具
- 新增 tests/ 测试代码（文本切分器 + Prompt 模板）
- 新增 .mcp.json MCP 配置
- 更新 README 增加 MCP 章节
- 更新 PROJECT_HIGHLIGHTS.md 面试话术"

git push origin feature/week2-fastapi-init
```

### 下午：写简历项目描述

```markdown
项目名：AI 智能知识库问答系统（RAG Chat + MCP Server） | 独立开发 | 2026.07

技术栈：FastAPI + Chroma + Redis + SQLAlchemy + MCP + DeepSeek API + SSE

项目描述：
基于 RAG 架构的智能问答系统，支持文档入库、向量语义检索、多轮对话记忆与
流式响应。通过 MCP 协议将知识库能力包装为 Agent 可调用的工具集合，
使 Claude Code 等 AI Agent 能直接操作知识库。

核心工作：
· 自研递归文本切分器，支持优先分隔符策略与重叠窗口，保证语义完整性
· 集成 Chroma 向量库实现余弦相似度检索，设计 distance 阈值 + 来源多样性去重
· 搭建 Redis 短期记忆管道（LPUSH + EXPIRE），实现 30 分钟自动过期的多轮对话上下文
· 实现 SSE 流式响应，双层 finally + CancelledError 确保客户端断连时资源正确释放
· 编写 MCP Server，将 RAG 检索/入库能力暴露为 Agent 可调用的标准化工具
· 编写单元测试（pytest），使用 Claude Code + 自定义 Skill 辅助开发全流程
```

---

## 第 7 天：模拟面试练习

**目标：** 把「做的东西」翻译成「能讲出来的东西」。

### 上午：对着架构图用嘴讲一遍（1 小时）

找面墙（或对着镜子），讲下面这段话，**录音**，然后自己听一遍：

> 「我做的是一个 RAG 智能知识库系统。核心流程：用户发问题 → API Key 鉴权 → 从 Redis 拉历史对话 + Chroma 向量检索相关文档 → 拼成 Prompt 发 DeepSeek → SSE 流式逐字推给前端 → 写入 Redis 和 SQLite。
>
> 最有意思的部分是——我把它做成了两用。它既可以独立运行（FastAPI + chat.html），也可以作为 MCP Server 被 Claude Code 调用。比如在 Claude Code 里说"搜索我的知识库"，它就会调我的向量检索工具，返回相关文档再回答。
>
> 这个经历让我理解了：Agent 是大脑，MCP 是手脚。Agent 不需要知道工具内部怎么实现的，只要遵循 MCP 协议，就能统一调用。」

### 下午：用 Claude Code 做模拟面试（1 小时）

在 Claude Code 里发：

```
你现在扮演一家 AI 应用公司的后端面试官，面试 AI 智能体应用开发实习生。
我说了我做过「基于 RAG 的智能知识库系统 + MCP Server」，
请你针对这个项目提 5 个追问，越细越好。
我回答完一个你再问下一个，每个回答之后给我评价。
```

重点准备 MCP 相关追问：
- "MCP 和传统的 REST API 有什么区别？"
- "为什么选 MCP 而不是直接用 Function Calling？"
- "MCP Server 的工具粒度怎么设计？"

---

## 这一周结束后的成果

| 产出 | 用途 |
|------|------|
| 完整 README（架构图+能力表+快速开始+MCP 章节） | GitHub 门面 ✅ |
| `static/chat.html` 前端页面 | 面试演示 ✅ |
| `mcp_server/` MCP 工具化（search/ingest/list） | 简历核心亮点 |
| `.mcp.json` MCP 配置 | Claude Code 一键连接 |
| `tests/` 单元测试 | 证明代码质量 |
| `PROJECT_HIGHLIGHTS.md` | 面试前 10 分钟看一遍 |
| 干净的 Git 历史 | 专业 |
| Claude Code 自定义 Skill | 面试能聊 Agent 产品 |
| 简历项目描述（6 条核心工作） | 直接复制粘贴 |
| 模拟面试问答 | 查漏补缺 |

---

> 💡 **三大升级（相比原计划）：**
> 1. MCP 从「玩具 demo」→「把 RAG 系统能力工具化」，简历含金量翻倍
> 2. 补了单元测试，面试不会被动
> 3. 项目形态从「一个后端服务」→「独立服务 + Agent 工具」双形态，更匹配 AI 智能体岗位
>
> 每天做完一项，把上面进度表的状态改了。卡住了就问 Claude Code。
